# 交接文档：小漓微信视觉后端（第 3 轮交接）

> 本文档给下一个 AI 接续推进用。当前 HEAD=`c14c59c`，289 项测试全绿，已打包 `dist\小漓\`（17:21）。
> 微信版本约束（延续，不得违反）：**不降级微信**，必须在 4.1.12.51 上完成。数据库解密路线已放弃。
> 唯一可用通道：visual（PrintWindow + RapidOCR + pyautogui）；wxauto4 结构性失效，勿依赖。

## 一、本轮工作（上一轮交接之后的所有改动，按 commit 时间）

上一轮交接停在 `d388b1c`（截图前置前 + 诊断日志）。本轮从头到尾解决的问题链：

1. **输入框"发送"按钮被 OCR 当消息顶掉真实最新消息**（`be3ec32`）：消息区 OCR 把输入框右下"发送"按钮读成消息（x 靠右判 self），且它排最下 → `latest` 硬取 `msgs[-1]` 就是它 → 整会话跳过，用户消息永不处理。修复：`_flush` 右下角按钮噪声过滤（text=='发送' 且 x>0.85w、y>0.8h）+ `process_new_messages` 用 `last_foreign` 记最后一条非 self 消息作 latest。
2. **红圈轮询一直把微信拉前台**（`c2e97c1`）：`_refresh` 无条件 `_foreground()`。修复：`_refresh(force, foreground)` 加参数，`iter_unread_with_type` 红圈检测传 `foreground=False` 后台静默截图（用户实测后台能识别红圈）；点击/读取/发送仍置前。
3. **私聊 sender='未知'**（`c2e97c1`）：`_flush` 里左侧消息 sender 硬编码"未知"，改为 `chat`（私聊发送人=会话名）。
4. **中文发送变「（）」**（`c2e97c1`）：`pyautogui.typewrite` 逐键模拟对中文无效（无对应键位，输入法拦截成括号）。改 `pyperclip.copy` + Ctrl+V + Enter。
5. **群聊 sender 识别**（`cbf2af6`）：群聊气泡上方发送者名（短文本行）被当独立消息。`get_messages` 引入 `pending_name` 候选机制——对方短文本行（≤8 字符）先作候选，后续内容行 y 差<150 消费为发送者名；悬空候选回退独立短消息；self（右侧）排除。
6. **标题区 OCR + 群聊判定权威信号**（`c1bf3a2` + `e696d24` + `e81176d`）：
   - 新增第三 OCR 区域（标题区）：`read_title()` 读右侧会话标题，`parse_title()` 解析——标题带括号人数（`'强盗"集团(5)'`）= 群聊，否则私聊。
   - `is_group_chat(chat_name, title=None)`：title 优先判群聊，替代「群/集团」名称启发式（普通群名如'哆菈A夢'会漏判）；缺省回退启发式。
   - 群聊 @ 过滤：`process_new_messages` 群聊消息未 `@小漓` 一律不回复（私聊不限）；`_handle_text` 群聊判定走标题。
   - `pick_ocr_region.py` 支持三区域框选（列表→消息→标题）+ `--title` 手填。
   - 三个区域值已同步代码默认（`_SESSION/_MESSAGE/_TITLE_REGION_RATIO`），打包 exe 无 json 时用。
7. **read_title 拼接分段标题**（`574c29e`）：OCR 把标题拆多段（`'"强盗"'` + `'集团(5)'`），旧实现只取最长单行缺左半。改区域内文本按 x 排序拼接。
8. **toggle 兜底**（`e81176d`）：`get_messages` 读标题 None 时 `_switch_chat(chat, force=True)` 重切恢复选中再读（与读 0 条重试同模式）；`read_title(foreground)` 参数——处理新消息路径置前截图防遮挡黑图。
9. **头像锚定标定**（`c14c59c`）：`_detect_self_avatar_ys` 的 `base_h` 从 `rh//12` 改 `rh//18`（真机实测头像≈消息区 1/17.5，旧假设偏大导致多尺度脱靶）；`avatar_template` 已写入 `xiaoli_desktop\config.json`（用户头像：`D:\AI\小漓\屏幕截图 2026-08-13 124316.png`，66x66 粉色卡通图）。sender 判定优先头像锚定（y 对齐即 self），x 中线仅降级。

## 二、关键代码位置（file:line 为本轮核实，改动后需重查）

| 位置 | 作用 |
|---|---|
| `visual_backend.py` `_refresh(force, foreground)` | 截图；foreground=False 后台静默（红圈轮询） |
| `visual_backend.py` `iter_unread_with_type` | 红圈像素检测 → 会话（后台截图） |
| `visual_backend.py` `read_title(foreground)` / `parse_title` | 标题区 OCR + 群聊判定（括号人数） |
| `visual_backend.py` `get_messages` | 读标题（None 时 force 重切）+ `_flush`（按钮过滤 / 群聊 sender） |
| `visual_backend.py` `_detect_self_avatar_ys` | 头像锚定（base_h=rh//18，多尺度 0.7-1.3） |
| `visual_backend.py` `_SESSION/_MESSAGE/_TITLE_REGION_RATIO` | 默认三区域（用户圈选值） |
| `xiaoli_bot.py` `process_new_messages` | last_foreign + 群聊 @ 过滤 |
| `xiaoli_bot.py` `_handle_text` | 群聊判定走 `self.wx._current_is_group` |
| `wechat_bot.py` `is_group_chat(chat_name, title=None)` | 标题优先判群聊 |
| `tools\pick_ocr_region.py` | 三区域框选脚本 |
| `xiaoli_desktop\wx_ocr_region.json` | 三区域配置（session/message/title） |
| `xiaoli_desktop\config.json` | avatar_template（用户头像路径） |

## 三、遗留问题（下一个 AI 的工作，按优先级）

1. **【最高优先】用户级端到端验收未完成**：跑 `dist\小漓\小漓.exe` → ① 给王文生发消息，看 `[最新消息]` sender 是否稳定为对方（头像锚定后不再因 x 波动误判 self）+ bot 回复；② 群聊发无 @ 消息看 `[跳过] 群聊消息未 @小漓` 不回复、发 `@小漓` 看回复。avatar_template 是**用户机器绝对路径**，换机器/换用户需重新上传头像。
2. **头像模板质量**：66x66 匹配 0.778（阈值 0.6），有富余但不高；若某些会话背景干扰脱靶，建议换 200px+ 高清头像截图。
3. **群聊 @ 消息区 OCR 未真机验证**：会话列表预览行读到过 `@小漓`（可读），但消息区内 @ 消息的 OCR 格式未实测（群里没有 @ 消息样本）。
4. **标题区左边界仍切标题左半**：标题区 x 0.4136-0.807，标题实际 x 0.444-0.577 在区内，但 read_title 依赖 OCR 分段拼接；若要用标题做**会话名权威来源**（根治 toggle 的 `_current_chat` 漂移、重启后第一次点击取消选中），需重圈标题区左边界到 x≈0.35。
5. **群聊发送者名启发式边界**：≤8 字符 + y 差<150 识别发送者名，长昵称（>8 字）读不到、两条紧贴短消息可能误配——真机数据（名字-内容 106px、块间 186px）定的阈值，遇误判调参。
6. **bot.log 打包路径**：exe 下日志写 `dist\小漓\_internal\bot.log`（源码时在 `xiaoli_desktop\bot.log`）；首页「运行日志」区读的路径在打包后解析到 `_internal`，**日志区能否显示未实测**——不显示需修 `_BOT_LOG_PATH`（改成相对 exe 目录而非 `__file__` 三级上溯）。
7. **普通用户分发**：三区域是相对比例绑窗口尺寸（1300x1610 标定）；普通用户窗口不同会错位——分发前需首启框选引导（把 pick_ocr_region 嵌入 exe 首次运行）或自适应检测。
8. **取消选中根治**：`_switch_chat` 仍靠进程内 `_current_chat` 记忆，重启后清空会多一次点击（有 None/读 0 条重试兜底不卡死）；根治方向：点击前 `read_title` 读真实会话名，已选中不点。

## 四、踩坑教训（下一个 AI 务必记住）

1. **实机测试探针别 force 反复点同一会话**——微信会话列表是 toggle，force 连续点击第 2 次会**取消选中**，消息区/标题区读空是探针自造的副作用，不是产品 bug。真机验证要「先切到别的会话再切回来」。
2. **wx_ocr_region.json 是相对比例绑窗口尺寸**：窗口大小变了区域就错位；打包 exe 不读 json（路径解析失败回退代码默认），**改区域默认值必须同步代码常量**。
3. **typewrite 对中文无效**（输入法拦截成「（）」），中文发送必须剪贴板（pyperclip + Ctrl+V）。
4. **红圈轮询必须后台**（不置前打扰用户），置前只在点击/读取/发送前。
5. **PrintWindow 被完全遮挡时黑图**：`read_title` 在处理新消息的路径（get_messages）要置前截图。
6. **头像匹配 base_h 必须真机标定**：`rh//12` 是错的（偏大 40%+ 脱靶），实测 `rh//18`。
7. **OCR 把标题/发送者名拆多段**：拼接（按 x 排序）而非取最长单行——`'"强盗"'`+`'集团(5)'` 分开是常态。
8. **`process_new_messages` 有基类（wechat_bot.py，逐条遍历）和 AgentBot 覆写（xiaoli_bot.py）两个版本**，真机走 AgentBot；写测试必须建 AgentBot 实例，建 WeChatBot 会测到基类。

## 五、测试 / 打包 / 运行

- **测试**：`cd D:\AI\小漓 && .venv\Scripts\python.exe -m unittest discover -s xiaoli_desktop\tests -p "test_*.py"`（当前 289 项全绿）。
- **真机读消息**：`.venv\Scripts\python.exe tools\test_read_messages.py 王文生`（读会话消息打印 sender/content）。
- **框选区域**：`.venv\Scripts\python.exe tools\pick_ocr_region.py`（拖三框：列表区→消息区→标题区；右键撤销；45s 超时）。
- **打包**：`.venv\Scripts\python.exe -m PyInstaller 小漓.spec --noconfirm` → `dist\小漓\`。
- **运行**：`dist\小漓\小漓.exe`；日志 `dist\小漓\_internal\bot.log`。

## 六、环境依赖

- Python 3.12.9，venv `D:\AI\小漓\.venv`。
- 关键依赖：`rapidocr-onnxruntime==1.4.4`、`onnxruntime`、`opencv-python`、`numpy`、`PySide6`、`pyautogui`、`pyperclip`、`uiautomation`、`Pillow`、`wxauto4`（仅降级死路，勿依赖）。
- PyInstaller 6.21.0。

## 七、不要做的事

- 不要降级微信、不要回头走数据库解密路线、不要用 Windows OCR（winsdk）。
- 不要依赖 wxauto4（4.1.12 结构性失效）。
- 不要用像素亮度判断「黑屏」（深色主题会误判）。
- **实机探针不要 force 连点同一会话**（toggle 取消选中，读空是自造的）。
