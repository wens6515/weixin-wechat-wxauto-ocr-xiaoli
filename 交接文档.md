# 交接文档：微信 4.1.12.51 消息获取——hook 密钥提取通道探索

> 本文件为**下一个 AI 接续任务**（team 模式会话）的交接说明。
> 交接时间：2026-08（破军域会话）
> 前序背景：小漓微信机器人（xiaoli_desktop\wechat_bot.py）当前用 PrintWindow 截图 + OCR 视觉方案，**无法辨别消息类型**（图片/视频/文件消息在 OCR 眼里不可见，用户确认 OCR 判类型/发送方不可靠）。用户要求：**彻底去除 OCR**，改为从微信底层获取"王文生"发送的消息（结构化类型/发送方/内容）。

---

## 1. 任务目标（一句话）

在小漓微信机器人上，**移除 OCR 接收链路**，改为通过**内存密钥提取 + SQLCipher 数据库解密**读取微信 4.1.12.51 的消息（结构化：类型/发送方/内容），以"王文生"会话为验证目标。

## 2. 当前状态总览（诚实评估）

| 环节 | 状态 | 证据 |
|---|---|---|
| 本地数据库识别 | ✅ 完成 | message_0.db 等 17 库确认为 SQLCipher 4 加密（非旧结论的 mmkv） |
| hook 注入通道 | ✅ 打通 | frida spawn/attach 微信均成功，能 hook Weixin.dll 内部函数 |
| **数据库主密钥获取** | ❌ **未成功** | 6 种尝试全部失败，原因已定位（见 §4） |
| 数据库解密 | ⏸ 待密钥 | decrypt_all.py 已就绪，HMAC 自验证 |
| 读取"王文生"消息 | ⏸ 待解密 | export_msgs.py 已就绪（含 sender 逐库验证） |

**一句话卡点**：微信 4.1.12.51 的 codec 配置函数（设置数据库主密码处）偏移未知——社区工具（wechat-decrypt / stargazer-2026）验证的是 4.1.12.26 及以下，偏移 0x3486140 在本版本已失效。需要**逆向定位新偏移**或**降级微信**。

---

## 3. 已验证事实（带证据，避免重复踩坑）

### 3.1 数据目录结构（本机实测）

微信 4.1.12.51 数据在 `D:\微信文件\xwechat_files\`（**不是** APPDATA）：

```
xwechat_files/
├── all_users/            # 公共配置（config/login/sqlite/head_imgs）
├── Backup/               # 备份（空）
└── wxid_wkfx633inecp22_04c0/   # 当前账号"小漓"（另有 wxid_ipq0oio3fp7c22_d150 旧账号）
    ├── db_storage/       # ★ 数据库核心
    │   ├── MMKV/         # 仅少量 4096B 文件（tinfo/storage_common）——消息主体不在 mmkv
    │   ├── message/      # ★ message_0.db (15MB) + biz_message_0.db + media_0.db + message_fts.db
    │   ├── session/      # ★ session.db（会话列表+未读数）
    │   ├── contact/      # ★ contact.db + contact_fts.db
    │   ├── favorite/ emoticon/ sns/ bizchat/ hardlink/ head_image/ general/ solitaire/
    ├── msg/              # 附件：attach/{md5}/ + file/2026-06/ + video/
    └── config/ cache/ business/ temp/ resource/ apm_record/
```

### 3.2 SQLCipher 4 加密细节（README 实证 + 本机验证）

- **算法**：AES-256-CBC + HMAC-SHA512；KDF：PBKDF2-HMAC-SHA512 256,000 迭代
- **页面**：4096 字节，reserve=80（IV 16B @ offset 4016，HMAC 64B @ offset 4032）
- **布局**：`[0:16]=salt | [16:4016]=加密数据 | [4016:4032]=IV | [4032:4096]=HMAC`
- **每个数据库独立 salt**（文件头 16 字节），同一 password 逐库派生 enc_key
- **HMAC 验证**（可用它判断候选密钥真伪）：
  ```python
  mac_salt = salt XOR 0x3a
  mac_key = PBKDF2-HMAC-SHA512(enc_key, mac_salt, 2, 32)
  valid = HMAC-SHA512(mac_key, page1[16:4032] + pack('<I', 1)) == page1[4032:4096]
  ```
- **本机关键 salt**：message_0.db = `f4b6aca23b0553f77c157b7fc8283712`；session.db = `e4d191d168c657c347a0880463e03446`

### 3.3 社区方案实证（GitHub API 2026-08 可访问）

| 项目 | 状态 | 对本机价值 |
|---|---|---|
| **328336690/wechat-decrypt**（16★） | 活跃，README 完整 | 内存扫描 `x'<hex>'` 模式——**本机 0 命中**（格式已变） |
| **stargazer-2026/wechat-4.1.12-decrypt**（4★）★ | **专为 4.1.12 写**，含 Frida spawn hook 方案 | **核心参考**：README 给出 4.1.12.26 的 codec 偏移 0x3486140 + 完整解密逻辑；25/25 全解 |
| yinhuacha869/ReadWxKey（4★） | 闭源 exe，声明支持 4.1.8+ 及以上 | 下载了 ReadWxKey.exe 但启动方式不明（"启动方式错误"），放弃 |
| ycccccccy/wx_key（1791★） | **被腾讯 DMCA 下架** | 可行性最强反证（腾讯用法律封杀=功能真实存在） |
| mrsanshui/WeChatPYAPI（1046★） | 专业版支持 4.1.2.16（非 4.1.12.51） | DLL hook 路线，版本绑定+付费+关杀软，不推荐为主通道 |

### 3.4 本机微信环境

- 版本：4.1.12.51，安装 `D:\腾讯\Weixin\Weixin.exe`（目录 `D:\腾讯\Weixin\4.1.12.51\`）
- 主进程 Weixin.exe（5 进程架构），主 DLL `Weixin.dll`（185.5MB，image_base 0x180000000）
- 登录账号"小漓"（wxid_wkfx633inecp22_04c0）；**重启自动登录**（key_info.dat 保留，cdp_probe 实测 3 次重启 login_ok=true）
- 数据目录经微信"设置→文件管理"确认：`D:\微信文件\xwechat_files\`

---

## 4. 密钥获取尝试全记录（6 次失败，team 会话必读）

### 4.1 尝试 1：wechat-decrypt 内存扫描（hex 文本格式）
- **方法**：扫 5 个进程全部可读内存，找 `x'<64hex_enc_key><32hex_salt>'` 文本模式
- **结果**：`0 hex patterns`——5 进程 1GB+ 内存 0 命中
- **结论**：4.1.12.51 不再以该文本格式缓存密钥（4.1.11+ 格式变更，README 已证实）

### 4.2 尝试 2：二进制 salt 探针
- **方法**：在 5 进程内存搜二进制 salt（`f4b6aca2...`）出现位置，其前 32 字节当 enc_key 验证
- **结果**：0 命中（salt 二进制也不在可读内存）
- **结论**：密钥不在进程可读内存区（或受保护）

### 4.3 尝试 3：attach 运行中微信 hook（不重启）
- **方法**：frida attach 主进程，hook 0x3526E70（当时误判为 codec 函数），等 60s
- **结果**：hook 成功但 60 秒零触发
- **结论**：密钥只在启动早期设置，运行中不重复调用 → **attach 错过，必须 spawn**

### 4.4 尝试 4：spawn + Memory.readByteArray（第一次 spawn）
- **方法**：spawn 微信，hook 0x3526E70，JS 读 rcx 前 64 字节
- **结果**：hook 成功且**启动早期触发几十次**（rcx 地址交替），但 JS 报 `TypeError: not a function`
- **根因**：**frida 17 已移除 `Memory.readByteArray`**——必须用 `c.rcx.readByteArray()`（NativePointer 方法）。⚠️ 这是关键 API 差异，README 用 frida 15，本机装 17.17.0

### 4.5 尝试 5：spawn + readByteArray 读 rcx 前 32 字节当 password
- **方法**：修好 API 后 spawn，每个候选 PBKDF2 派生 + HMAC 验证 message_0.db
- **结果**：捕获大量候选（每次触发 pwd 都不同，rcx 在两地址差 0x30 交替），**全部验证失败**
- **数据规律**：调用1 的 rcx[32:64] == 调用2 的 rcx[0:32]——**同一结构体交替读取**，捕获的是**页面级/消息级密钥**，不是数据库主密码

### 4.6 尝试 6：spawn + ASCII `x'<hex>'` 文本解析 + README 偏移验证
- **方法 A（文本解析）**：读 rcx 指向 512 字节 hex，搜 `7827`（ASCII "x'"），提取 `x'<96hex>'`
- **结果**：捕获 21 个候选，解码后 salt 全是 **8 字节级短密钥**（如 `66ca7cb978624c45`），不匹配任何真实 db salt（16 字节）
- **方法 B（README 偏移 0x3486140）**：直接 hook 0x3486140 → **`unable to intercept function`**（该地址在 4.1.12.51 上不是有效函数入口，偏移已失效）

### 4.7 关键反汇编结论（为什么 0x3526E70 不是 codec 函数）

`0x3526E70` 函数体：`sub rsp,0x48` → 存 rcx/rdx 到栈 → `lea` 加载 4 个字符串（含 MMV1-B @ 0x8FAF888）→ `call rax`（间接调用）→ 返回。**这是日志/格式化/分发函数**——rcx 只是格式化参数，不是"结构前 32 字节=password"。

- 0x3526E70 引用的是 **MMV1-B**（0x8FAF888），有 **16 个 call xref**（0x35268A9 ~ 0x3527D81）——被广泛调用，更证非 codec 专用
- 本机 MMV1 字符串两处：MMV1-A @ 文件偏移 0x7012E44（RVA 0x7013A44）、MMV1-B @ 0x8FAF888（RVA 0x8FB0888）——**均无 movabs xref**

---

## 5. 下一步路线图（team 会话候选方案，按优先级）

### 方案 A：逆向定位 4.1.12.51 的 codec 配置函数（推荐，工作量中-高）
README（4.1.12.26）说 codec 函数偏移 0x3486140、rcx 前 32 字节=password。4.1.12.51 需重新定位。**已定位的线索**：
- 16 处 `0x3E800`（256000 迭代数）立即数在 .text，其中 10 个定位到候选函数入口：
  `0xEACBE7, 0x1097896, 0x1097998, 0x10CEB9A, 0x1205130, 0x13C0380, 0x1BF3B5E, 0x2372006, 0x2A41DE3, 0x2B1D15F`
- **建议**：对候选函数反汇编，找"调用 PBKDF2 后把派生密钥写入内存结构"且"被数据库打开路径调用"的那个；用 IDA/Ghidra（用户可装）一次性定位最准
- **验证**：找到后 spawn + hook，rcx 前 32 字节当 password → PBKDF2 派生 enc_key → HMAC 验证 message_0.db

### 方案 B：降级微信到 4.1.12.26（最快，但违背"保持新版"）
- 4.1.12.26 的 codec 偏移已知（0x3486140），stargazer-2026 工具链现成
- **代价**：微信降级、之后升级再失效；需先确认降级包可得性（GitHub releases / 微信官方归档）
- **若用户可接受降级**：这是半小时出结果的路径

### 方案 C：hook 0x3E800 的 PBKDF2 候选函数（中等成功率）
- 不用完整逆向，对 10 个候选逐个 spawn hook，看哪个函数参数含 password（rcx/rdx 指向 32 字节密码）
- 每个候选一次 spawn（约 2 分钟），最多 10 次

### 拿到密钥后的固定流程（已备好工具）
```bash
# 1. 密钥已写入 key.txt（spawn_key*.py 的目标）
# 2. 解密全部库
cd D:\AI\小漓\.rivet\scratch\wechat-4.1.12-decrypt
D:\AI\小漓\.venv\Scripts\python.exe decrypt_all.py <password_hex> D:\微信文件\xwechat_files\wxid_wkfx633inecp22_04c0\db_storage decrypted
# 3. 导出"王文生"消息（注意：sender 映射逐库不同！message_0 用 Name2Id rowid，旧库用 1/2 且可能相反——必须锚点句验证）
D:\AI\小漓\.venv\Scripts\python.exe export_msgs.py
# 4. 实现 wx_backend\memory_backend.py 接入 wechat_bot（Protocol: iter_unread_sessions/get_messages/send_text/send_file）
#    priority: memory(1) > visual(10) > wxauto(100)；发送仍走 visual（输入框点击），接收走内存通道
```

---

## 6. 环境与资源（team 会话直接可用）

### 6.1 已装依赖（根 .venv `D:\AI\小漓\.venv`）
- **frida 17.17.0** ⚠️（`Memory.readByteArray` 已移除，用 `ptr.readByteArray()`）
- **pycryptodome 3.23.0**、**capstone 5.0.9**、winsdk 1.0.0b10、PySide6 6.11.1
- Python 3.12.9；测试用根 `.venv\Scripts\python.exe`（逐个模块跑，`discover` 报 `__file__` None）

### 6.2 关键脚本清单（.rivet\scratch\）

| 脚本 | 作用 | 状态 |
|---|---|---|
| `wechat-decrypt\`（克隆） | 原版内存扫描；`find_all_keys.py` 改过 5 进程版 | hex 模式本机失效 |
| `wechat-4.1.12-decrypt\`（克隆）★ | **stargazer-2026 工具链**：find_key.py（Frida spawn）/ decrypt_all.py / export_msgs.py | find_key.py 偏移已改 0x3526E70（无效），decrypt/export 直接可用 |
| `spawn_key.py` / `spawn_key2.py` / `spawn_key3.py` | 本会话迭代的 spawn+hook 脚本（ASCII 文本/二进制 salt 搜索） | 均未命中，但框架可复用 |
| `attach_key.py` / `attach_key2.py` | attach 版（无需重启） | 已证 attach 错过启动期 |
| `probe_3486140.py` | hook README 偏移验证 | 证 0x3486140 本机失效 |
| `probe_binary_salt.py` / `mem_probe.py` | 内存搜索探针 | 0 命中 |
| `ReadWxKey.exe` | 闭源工具（4.1.12.x 支持） | 启动方式不明，待查 |
| `*_readme.md` | 各仓库 README 存档 | 参考 |

### 6.3 网络访问
- **web_search / web_fetch TLS 证书失败**（本环境）；**curl -sk 可访问 GitHub API**（api.github.com 的 readme 接口返回 base64，raw.githubusercontent 不稳定）
- 后续调研：`curl -sk "https://api.github.com/repos/xxx/readme"` + base64 解码

### 6.4 微信当前状态
- **已关闭**（本会话结束时 spawn 尝试的微信已退出，无残留进程）
- 数据完整（17 个加密库 + 附件）

---

## 7. 测试与验证现状（本仓库既有）

- 交接文档 v1 记录的 9 模块测试全绿：visual 35 + wx_backend 39 + wechat_bot 24 + task_pause/attachment + config_store + engine + card_store
- 本会话**未改动任何生产代码**（全部探索在 .rivet\scratch\），无测试回归风险
- 交接文档.md 在本会话被删除（git status 显示 D）——本文件即重建版

---

## 8. 给 team 会话的启动建议

1. **先读**：本文档 §3-4（事实+失败教训）→ `docs\微信4本地数据破解与hook通道调研.md`（调研全貌）→ `.rivet\scratch\wx4112_readme.md`（stargazer-2026 的完整方案）
2. **决策点**：问用户选方案 A（逆向定位，保持新版）/ B（降级 4.1.12.26，最快）/ C（候选函数盲试）——方案 A 需要用户确认是否可装 IDA/Ghidra
3. **不要重复**：内存扫描（0 命中）、attach（错过启动期）、0x3526E70（非 codec 函数）、0x3486140（已失效）
4. **关键 API**：frida 17 用 `c.rcx.readByteArray()`；验证候选用 HMAC 逻辑（§3.2）
5. **成功后**：decrypt_all.py → export_msgs.py → 实现 memory_backend 接入 wechat_bot（§5 固定流程）
