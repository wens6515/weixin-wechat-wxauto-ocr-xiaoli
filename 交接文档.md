# 交接文档：视觉后端消息链路修复现状 + 待办清单

> 本文档给下一个 AI 接续推进用。当前 HEAD=`d388b1c`，278 项测试全绿，已打包到 `dist\小漓\`。
> 本轮核心成果：**定位并修复了「启动 bot 后收到新消息毫无反应」的完整根因链**（6 个 commit），
> 并把首页空白区改造成实时运行日志区。但「用户级验收」未完成——真正的端到端跑通还需真机验证。
>
> 微信版本约束（延续，不得违反）：**不降级微信**，必须在 4.1.12.51 上完成。数据库解密路线已放弃。

## 一、本轮修复的根因链（按发现顺序，全是真机实测定位）

「收到新消息毫无反应」不是一个原因，是**一串连锁问题**，逐个拔掉才通。按时间顺序：

1. **AgentBot 漏迁移旧 wxauto API**（commit `8ae4985`）：`AgentBot.process_new_messages` 及辅助方法还在调旧 wxauto 命名（`self.wx.GetSession()/ChatWith()/GetAllMessage()/SendFiles()` + `ImageMessage/FileMessage` isinstance），但后端已迁移到协议化 `WeChatBackend`（只有 `iter_unread_sessions/get_messages/send_text/send_file`）。旧 API 全部 AttributeError，被 `except Exception` 静默吞掉只写 bot.log。已改为新协议 + `MessageType` 枚举判断。

2. **窗口前置**（`cb826ae`）：visual 后端的点击/输入用 pyautogui，但从不把微信窗口置前，操作会发到前台窗口。加了 `_foreground()`（ctypes SetForegroundWindow + Alt 空击解前台锁 + SW_RESTORE）。

3. **会话列表 toggle 行为**（`bbf7308`）：微信会话列表点已选中的会话会**取消选中**、右侧消息区变空。`_switch_chat` 加 `_current_chat` 状态记忆，已选中则不重复点击。

4. **单字符消息误杀**（`cdb964d`）：`get_messages` 里的 `_is_noise` 用 `len(text) < 2` 把单字符（如「1」「好」）全当噪声过滤。用户实测发「1」是真实消息。已删掉这条规则，只保留「纯标点符号才算噪声」。

5. **读空兜底重试**（`ff501e6`）：`get_messages` 读到 0 条时，强制再点一次会话（绕过 `_current_chat` 判断）重新读。

6. **最终根因——截图前未置前微信**（`d388b1c`）：这是最致命的一环。**微信窗口被天枢界面（或其他窗口）完全遮挡时，PrintWindow 返回黑图**（微信 Chromium 渲染在被遮挡时失效）。之前所有「读到 0 条」的真实根源都是它，而误诊断成了「屏幕黑屏」。修复：`_refresh` 截图前先 `_foreground` 置前微信；`_foreground` 加「已前台则快速返回」避免每轮轮询打断用户。

## 二、关键代码位置（file:line，本轮已核实）

| 位置 | 作用 |
|---|---|
| `xiaoli_desktop\wx_backend\visual_backend.py:486` | `connect`：PrintWindow 截图连接（不移动窗口） |
| `xiaoli_desktop\wx_backend\visual_backend.py:501` | `_foreground`：微信置前（已前台快速返回 + Alt 空击 + SW_RESTORE） |
| `xiaoli_desktop\wx_backend\visual_backend.py:531` | `_refresh`：截图（**截图前先 _foreground**，根因修复点） |
| `xiaoli_desktop\wx_backend\visual_backend.py:658` | `iter_unread_sessions`：红圈检测 → 未读会话 |
| `xiaoli_desktop\wx_backend\visual_backend.py:720` | `_anchor_badge`：红圈锚定 fallback |
| `xiaoli_desktop\wx_backend\visual_backend.py:749` | `_switch_chat(chat, force=False)`：切会话（`_current_chat` 防重复点击，force 绕过） |
| `xiaoli_desktop\wx_backend\visual_backend.py:810` | `get_messages`：读消息（读空则 force 重点一次兜底） |
| `xiaoli_desktop\wx_backend\visual_backend.py:867` | `_is_noise`：噪声过滤（单字符已保留） |
| `xiaoli_desktop\wx_backend\visual_backend.py:929` | `send_text`：输入框点击 + typewrite + Enter |
| `xiaoli_desktop\wx_backend\visual_backend.py:951` | `_input_box_rect`：输入框定位（优先读标定值，回退硬编码比例） |
| `xiaoli_desktop\xiaoli_bot.py:312` | `send_trigger_to_window`：首轮提示词发送（两次回车，间隔 0.2s） |
| `xiaoli_desktop\xiaoli_bot.py:732` | `_switch_to_chat`：AgentBot 发送前切会话（探测 `_switch_chat`，无则 fail-closed） |
| `xiaoli_desktop\xiaoli_bot.py:764` | `_send_file_clipboard`：剪贴板发文件（CF_HDROP + Ctrl+V） |
| `xiaoli_desktop\xiaoli_bot.py:1021` | `_handle_text`：文本消息统一处理（任务判断 → 投递 or 聊天） |
| `xiaoli_desktop\xiaoli_bot.py:1055` | `process_new_messages`：主循环（含诊断日志） |
| `xiaoli_desktop\xiaoli_app\ui\pages.py:22` | `_BOT_LOG_PATH`：bot.log 正确路径（修复了 LogPage 少算一层 dirname 的 bug） |
| `xiaoli_desktop\xiaoli_app\ui\pages.py:36` | `HomePage`：首页（下方「运行日志」卡片 + 轮询 bot.log） |
| `xiaoli_desktop\xiaoli_app\ui\pages.py:385` | `HomePage._poll_log`：日志区增量拉取 |

## 三、遗留问题（下一个 AI 的工作，按优先级）

### P0：sender 误判（最可能卡住 bot 回复）

真机实测读到的消息里，`'1'` 和 OCR 噪声 `'次风爪'` 的 sender 被误判成 `self`。`process_new_messages` 里 `if sender == "self": continue` 会跳过它们、不回复。**这是当前最可能让「bot 收到消息却不回复」的原因。**

根因：visual 后端的 sender 判定只有两档——`_detect_self_avatar_ys`（头像模板匹配，阈值 0.6 未真机标定）降级到 `first_x > midline_x`（x 坐标中线）。头像锚定未标定 + OCR 噪声靠右，就会误判 self。

debug 方式：跑 `tools\test_read_messages.py 王文生`，看打印的每条 `[sender] content`，找出哪些真实消息被误判 self，调头像模板匹配阈值/尺寸假设，或改 sender 判定逻辑。

### P1：头像锚定参数未真机标定（延续上一轮 P0）

`_detect_self_avatar_ys` 的匹配阈值 `0.6` 和尺寸假设「头像高度 ≈ 消息区 1/12」是默认值。用户已上传头像模板（`屏幕截图 2026-08-13 124316.png`），但没在真实消息区验证。与 P0 强相关，可一并修。

### P2：跨进程 toggle 未根治

`_current_chat` 是进程内记忆，bot 重启后清零。若重启后微信还停在目标会话，第一次 `get_messages` 会点一下取消选中——但现在有 `ff501e6` 的「读空强制重点」兜底，所以表现为「第一次读空、第二次自动恢复」，多一次点击但不会卡死。要根治需「点击前 OCR 顶部标题判断当前会话」，标题区精确坐标尚未标定准。

### P3：用户级验收未完成（blocked）

我做到了脚本探针级验证（`get_messages('王文生')` 稳定读到 6 条消息），但**没有让用户在真实环境跑打包后的 exe 验证端到端**。验收动作：运行 `dist\小漓\小漓.exe` → 微信发一条消息 → 看首页「运行日志」区是否依次出现「🔔 发现新消息」→「[读取] 读到 N 条」→「[MSG] [会话] 消息内容」→ bot 回复。这是下一个 AI 接手后的第一件事。

### P4：消息区类型只标注最新一条（延续）

`get_messages` 末尾只把 `msgs[-1]` 的类型改成 `_session_types[chat]`，更早消息硬编码 TEXT。当前够用，处理历史消息要改。

### P5：文件消息完整文件名（延续）

会话列表预览行文件名被微信 UI 截断（`.do..`），完整文件名要靠消息区 OCR 或接收目录主干匹配。

### P6：`[视频]`/`[表情]` 类型未真机验证（延续）

`_preview_type` 对这两个类型有单元测试，但真机只验证过 `[图片]`/`[文件]`/文字。上层 wechat_bot 类型分发只接 IMAGE/FILE/TEXT。

## 四、本轮踩坑教训（下一个 AI 务必记住，别重蹈）

1. **微信被遮挡时 PrintWindow 返回黑图**——截图前必须 `_foreground` 置前。这是「读到 0 条」的最常见根因，别再去怀疑「屏幕黑」。
2. **深色主题让「非白像素占比」误判**——我用 `(像素<200).mean()` 判断「黑屏」，结果天枢界面是深色主题，占比天然接近 1.0，误诊成「屏幕黑屏」被用户骂了。判断黑屏绝不能靠像素亮度，要直接看截图内容。
3. **微信会话列表是 toggle 行为**——点已选中的会取消选中。所有点击切换都要防重复。
4. **wxauto4 在微信 4.1.12 结构性失效**——`wxauto_backend.py` 是降级死路（connect 就失败），业务代码不要依赖它。唯一可用通道是 visual（PrintWindow + RapidOCR + pyautogui）。
5. **真机实测 > mock 测试**——visual 后端的链路（截图/OCR/点击/发送）mock 测试测不出真实问题，必须跑 `tools\test_read_messages.py` 真机验证。

## 五、测试 / 打包 / 运行

- **测试**：`cd D:\AI\小漓 && .venv\Scripts\python.exe -m unittest discover -s xiaoli_desktop\tests -p "test_*.py"`（当前 278 项全绿）。
- **单模块**：`... -m unittest xiaoli_desktop.tests.test_visual_backend`。
- **真机实测读取**：`cd D:\AI\小漓 && .venv\Scripts\python.exe tools\test_read_messages.py 王文生`（连接 visual 后端，读指定会话消息，打印 sender/content）。
- **输入框标定**：`.venv\Scripts\python.exe tools\calibrate_input_box.py`（鼠标移到输入框按 F8，存相对比例到 `wx_backend\wx_input_box.json`）。
- **打包**：`cd D:\AI\小漓 && .venv\Scripts\python.exe -m PyInstaller 小漓.spec --noconfirm` → `dist\小漓\`。spec 已配 `collect_data_files('rapidocr_onnxruntime')`（三个 onnx + config.yaml 必须打进 `_internal`）。
- **运行**：`dist\小漓\小漓.exe`（GUI）。首页下方「运行日志」卡片实时显示 bot.log。

## 六、环境依赖

- Python 3.12.9，venv `D:\AI\小漓\.venv`。
- 关键依赖：`rapidocr-onnxruntime==1.4.4`、`onnxruntime`、`opencv-python`、`numpy`、`PySide6`、`pyautogui`、`uiautomation`、`Pillow`、`wxauto4`（仅降级死路，勿依赖）。
- PyInstaller 6.21.0。

## 七、不要做的事

- 不要回头走数据库解密路线（已穷尽）。
- 不要用 Windows OCR（winsdk）——噪声致命。
- 不要降级微信。
- 不要依赖 wxauto4（4.1.12 结构性失效）。
- 不要用像素亮度判断「黑屏」（深色主题会误判）。
