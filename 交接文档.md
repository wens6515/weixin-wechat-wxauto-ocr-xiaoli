# 交接文档：小漓微信视觉后端（第 4 轮交接）

> 本文档给下一个 AI 接续推进用。当前 HEAD=`91f7e5f`，289 项测试全绿，已打包 `dist\小漓\`（含自定义图标）。
> 微信版本约束（延续，不得违反）：**不降级微信**，必须在 4.1.12.51 上完成。数据库解密路线已放弃。
> 唯一可用通道：visual（PrintWindow + RapidOCR + pyautogui）；wxauto4 结构性失效，勿依赖。

## 一、本轮工作（上一轮 `c14c59c` 之后的所有改动，按 commit 时间）

上一轮交接停在 `c14c59c`（头像锚定标定）。本轮从头到尾解决的问题链（5 个 commit）：

1. **模型清单更新 + 检测模型自动回填 + 首轮提示词回车加固**（`482579d`）：
   - `config_store.PRESET_PROVIDERS` 更新：deepseek 去掉已下线的 `deepseek-reasoner`、改为 `deepseek-v4-pro`（联网核实 DeepSeek 官方当前模型是 `deepseek-v4-flash` + `deepseek-v4-pro`）；zhipu 从 `glm-4.6/glm-4.6v` 改为 `glm-5.2/glm-5v-turbo`。
   - `ModelsPage._test`（「测试选中连接」）检测到可用模型后**自动写回**该 provider 的「模型」列（带 `厂商:模型` 前缀）+ 同步内存，点「保存并应用」持久化——不再只弹窗看一眼。
   - 首轮提示词发送 `send_prompt_to_tianshu` 的 hold 0.5s→2.0s（CLI 刚启动 TUI 未就绪时粘贴/回车会丢）；`send_trigger_to_window` 粘贴后等待按文本长度自适应（长文本 1.0s / 短指令 0.4s）。

2. **UI QSS 视觉增强**（`ce1976b`）：蓝紫渐变主色、圆角、层次（后续被主题系统重构吸收）。

3. **CLI 前台激活加固 + OCR 分片 + 5 主题壁纸毛玻璃 + 模型再补充**（`578dca6`）：
   - `/yes` 和「开始处理」唤起仍 hold=0.5 会失败 → `activate_window_by_title` 在 `SetActive` 之外补 `SetForegroundWindow` 强制前台；`/yes`（`send_yes_and_close`）和 `send_trigger_to_window` 唤起调用统一显式 `hold=2.0`。
   - **任务投递截断根因**：RapidOCR 检测模型 `limit_side_len=736`（min，探针实测 config.yaml），消息区 2x 后宽 1508px 会被内部压缩到 736 再检测，长文本行检测框不完整（真机 `task.json` raw_message 只投递前半段「镜头1：缓」）。修复：新增 `ocr_image_sharded`——消息区宽度超阈值时左右分片（12% 重叠）+ 去重合并，每片不超检测限制。
   - 智谱 models 补回 `glm-4.6v` + 新增免费 `glm-4v-flash`（联网确认：智谱首个完全免费图像理解模型，走标准 API、0 费用），默认 `vision_model` 改为 `zhipu:glm-4v-flash`。
   - 主题系统：`ui/__init__.py` 重构 `APP_QSS` 常量为 `THEMES`（5 套：blue 蓝紫/dark 暗夜深色/emerald 翡翠绿/sunset 落日橙/rose 樱花粉）+ `build_qss(theme, wallpaper)` 模板函数；毛玻璃用半透明卡片（rgba 0.88/0.82）；设置页加「界面主题与壁纸」区（切主题/选壁纸/清除/应用并持久化），`xiaoli_gui.py` 启动按 config 恢复。

4. **DeepSeek 酱角色卡 + 图标**（`26224ae`）：
   - `config_store.CARD_TEMPLATE` 默认卡 `system_prompt` 改为 DeepSeek 酱（蓝色大肥鱼）设定：蓝色小鲸鱼拟人、深蓝女仆装、蓝发鱼尾、白发带、聪明呆萌好奇、困惑歪头冒问号；emoji 🐟→🐳；颜文字风格带具体例子。`wechat_bot` 默认 `system_prompt` 同步。
   - `小漓.png`（960×960）转多尺寸 `小漓.ico`，`小漓.spec` 的 `EXE()` 加 `icon='小漓.ico'`。**注意：`小漓.spec` 和 `小漓.ico` 是 gitignore 的构建产物，不进 git，但已落盘、打包生效**。

5. **首启头像必选 + 设置页滚动 + 主题光晕**（`91f7e5f`）：
   - `FirstRunDialog` 头像从「（可选）…识别自己发的消息」改为「（必选）…区分消息是你发的还是别人发的」，「开始使用」接 `_on_accept` 校验（未选头像弹窗拦截）。
   - `SettingsPage` 整页套 `QScrollArea` 可滚动（设置项多不再被裁）；记忆框最大 200→最小 260、成果排除框 120→180；QSS 按钮 `min-height: 32px` + 更大 padding。
   - `build_qss` 无壁纸背景加径向渐变光晕（顶部主题色 alpha 0.16 → 底部背景色）。

## 二、关键代码位置（file:line 为本轮核实，改动后需重查）

| 位置 | 作用 |
|---|---|
| `visual_backend.py` `ocr_image_sharded(img)` | 消息区宽图左右分片 OCR（修复长文本截断） |
| `visual_backend.py` `get_messages` | 读标题 + `_flush`（按钮过滤/群聊 sender），消息区 OCR 走 `ocr_image_sharded` |
| `xiaoli_bot.py` `activate_window_by_title` | SetActive + SetForegroundWindow 双保险前台激活 |
| `xiaoli_bot.py` `send_trigger_to_window` | 粘贴后等待自适应文本长度 |
| `setup.py` `send_prompt_to_tianshu` / `send_yes_and_close` | hold=2.0 + enter_times=2 |
| `xiaoli_app/config_store.py` `PRESET_PROVIDERS` | 模型清单（deepseek flash/pro；zhipu 5.2/5v-turbo/4.6v/4v-flash） |
| `xiaoli_app/config_store.py` `CARD_TEMPLATE` | 默认角色卡（DeepSeek 酱设定） |
| `xiaoli_app/ui/__init__.py` `THEMES` / `build_qss` | 5 套主题 + 壁纸 + 光晕背景 |
| `xiaoli_app/ui/pages.py` `ModelsPage._test` / `_apply_fetched_models` | 检测模型自动回填 |
| `xiaoli_app/ui/pages.py` `SettingsPage` | QScrollArea 滚动 + 主题/壁纸选择 |
| `xiaoli_gui.py` `FirstRunDialog` / `_on_accept` | 首启头像必选校验 |
| `小漓.spec`（gitignore）`EXE(icon=...)` | 图标（小漓.ico） |
| `小漓.png` / `小漓.ico`（gitignore） | 图标源图 / 多尺寸 ico |

## 三、遗留问题（下一个 AI 的工作，按优先级）

1. **【最高优先】用户级端到端验收未完成**：跑 `dist\小漓\小漓.exe` → ① 首启头像必选拦截（不传头像点「开始使用」应弹窗）；② 初始化时 `/yes` 和首轮提示词是否真正提交到天枢 CLI（这轮是前台激活 + hold=2.0 加固，若仍失败要区分「粘贴进去没提交」还是「完全没粘上」）；③ 发一条长任务指令看 `task.json` 的 `raw_message` 是否完整（OCR 分片对长气泡的拼接）；④ 切 5 套主题 + 壁纸看渲染效果。
2. **OCR 分片拼接未真机验证**：`ocr_image_sharded` 分片 + 去重（按 `round(y/10)+text`）对真实长气泡的拼接效果未实测——若仍截断或漏字，需看分片边界（12% 重叠）是否够、去重键是否误删。
3. **主题 SVG 素材未落地**：用户要求「弄 SVG 美术素材」美化主题，本轮因 QtSvg 模块未装 + 网络代理故障（web_search/web_fetch 反复 timeout）改为纯 QSS 径向渐变光晕。下一步要么确认 QtSvg 依赖后用内联 SVG 装饰，要么 PIL 程序化生成装饰素材打进包，要么用户直接提供图片素材。
4. **打包清空 dist\小漓 数据副作用**：PyInstaller `--noconfirm` 的 COLLECT 会 `Removing dir dist\小漓` 再重建，每次打包都清掉 `cards\`/`config.json`（含 API key）/`memory.json`/`wxauto\`。测试时需重新填 key、重新走首启引导。若要保留数据需打包前备份。
5. **旧角色卡不自动更新**：`migrate_config` 幂等——已有 `cards\xiaoli.json` 不会覆盖。源码环境跑过旧版的，新设定不生效；需手动改人格设定或删卡重建（删除是破坏性操作）。
6. **头像必选后的引导验证**：首启引导里 `_on_accept` 校验 + 头像路径是用户机器绝对路径（换机器/换用户需重传）——延续第 3 轮的 avatar_template 约束。
7. **bot.log 打包路径**（延续）：exe 下日志写 `dist\小漓\_internal\bot.log`，首页「运行日志」区读的路径打包后解析到 `_internal`，能否显示未实测——不显示需修 `_BOT_LOG_PATH`。
8. **普通用户分发**（延续）：三区域是相对比例绑窗口尺寸（1300x1610 标定），普通用户窗口不同会错位——分发前需首启框选引导或自适应检测。
9. **取消选中根治**（延续）：`_switch_chat` 靠进程内 `_current_chat` 记忆，重启后清空会多一次点击；根治方向：点击前 `read_title` 读真实会话名。

## 四、踩坑教训（下一个 AI 务必记住）

1. **RapidOCR 检测模型 `limit_side_len=736`（min）**：任何 >736 宽的图会被内部压缩到 736 再检测，长文本行检测框不完整 → 内容截断。消息区 2x 后 1508px 必超，必须分片（见 `ocr_image_sharded`）。
2. **分片 OCR 坐标系坑**：第一版把 `region.resize(2x)` 移进分片函数，导致 `get_messages` 里 `midline_x = region.width//2` 用原始尺寸、而 OCR 坐标是 2x → 坐标错乱、群聊 sender 误判。正确做法：resize 留在调用方，分片函数接收 2x 图、返回 2x 坐标。
3. **QSS 测试契约**：`test_qss_sets_combobox_popup_color` 断言 `QComboBox QAbstractItemView` 文字色必须是 `#374151`（深色系统主题下防白字）。改主题色时下拉弹层文字色不能随便换，否则测试红。
4. **终端窗口激活**：uiautomation `SetActive()` 对终端类窗口（天枢 CLI 跑在 Windows Terminal/cmd）不够强，必须补 `SetForegroundWindow`，否则 Ctrl+V/回车发到错误窗口。
5. **`send_trigger_to_window` 的 hold 语义**：首启引导、任务唤起、首轮提示词三条路径 CLI 都可能「刚启动 TUI 未就绪」，hold 都要 2.0s，不能只给首轮提示词加。
6. **交付门禁 adopt**：本仓库多会话共享工作区，deliver_task 会把上轮会话的 owned 文件标为「historical owned」，提交前要 `adopt` 认领；跨区域大提交会触发内聚性门禁，需 `force=true` 或拆提交。
7. **打包必清空 dist\小漓**：见遗留问题 4。每次打包前想清楚用户数据要不要备份。
8. **迁移幂等**：`migrate_config` 已有 providers 就直接返回，改 `CARD_TEMPLATE` 默认卡不会覆盖已生成的卡（见遗留问题 5）。
9. **真机探针不要 force 连点同一会话**（延续）：toggle 取消选中是探针自造的副作用。
10. **中文发送必须剪贴板**（延续）：`typewrite` 对中文无效（输入法拦截成「（）」），必须 `pyperclip` + Ctrl+V。
11. **`process_new_messages` 有基类和 AgentBot 覆写两版**（延续）：真机走 AgentBot，写测试必须建 AgentBot 实例。

## 五、测试 / 打包 / 运行

- **测试**：`cd D:\AI\小漓 && .venv\Scripts\python.exe -m unittest discover -s xiaoli_desktop\tests -p "test_*.py"`（当前 289 项全绿）。
- **真机读消息**：`.venv\Scripts\python.exe tools\test_read_messages.py 王文生`（读会话消息打印 sender/content）。
- **框选区域**：`.venv\Scripts\python.exe tools\pick_ocr_region.py`（拖三框：列表区→消息区→标题区；右键撤销；45s 超时）。
- **打包**：`.venv\Scripts\python.exe -m PyInstaller 小漓.spec --noconfirm` → `dist\小漓\`（spec 已带 `icon='小漓.ico'`）。
- **运行**：`dist\小漓\小漓.exe`；日志 `dist\小漓\_internal\bot.log`。

## 六、环境依赖

- Python 3.12.9，venv `D:\AI\小漓\.venv`。
- 关键依赖：`rapidocr-onnxruntime==1.4.4`、`onnxruntime`、`opencv-python`、`numpy`、`PySide6`、`pyautogui`、`pyperclip`、`uiautomation`、`Pillow`、`wxauto4`（仅降级死路，勿依赖）。
- PyInstaller 6.21.0。
- **未装**：QtSvg（PySide6 的 SVG 模块）——主题要上 SVG 素材前需确认。

## 七、不要做的事

- 不要降级微信、不要回头走数据库解密路线、不要用 Windows OCR（winsdk）。
- 不要依赖 wxauto4（4.1.12 结构性失效）。
- 不要用像素亮度判断「黑屏」（深色主题会误判）。
- **实机探针不要 force 连点同一会话**（toggle 取消选中，读空是自造的）。
- 不要擅自删除用户机器上的 `cards\xiaoli.json`（破坏性操作，需确认）。
- 不要在没确认 QtSvg 依赖前给 QSS 上 SVG 背景图（静默失效或报错）。
