# 小漓 交接文档

> 状态：微信消息链路稳定 + 界面美化已完成一轮大改造（主题系统/背景粒子/透明度/字号/字体/壁纸库），用户实测反馈中，下一任 AI 继续界面优化。本文档供无缝接手。

## 一句话现状

小漓是 Windows 微信 AI 机器人桌面版（PySide6 GUI + 视觉后端自动化微信 + LLM 聊天 + 天枢任务桥）。微信消息定位已重构稳定（头像几何不变量），前端 UI 从「5 套扁平配色」升级为「12 套精选主题 + 粒子背景 + 可调透明度/字号 + 内置壁纸库 + 思源黑体」，已打包 `dist\小漓\` 标准位置（当前 HEAD：54ead23）。**下一步：根据用户实测反馈继续界面优化**。

## 项目结构速览

```
小漓/
├── xiaoli_desktop/
│   ├── xiaoli_gui.py          # GUI 入口（PySide6，托盘常驻；加载字体 + 应用主题）
│   ├── xiaoli_bot.py          # 合并版 bot：聊天 + 天枢任务桥（实际运行的是它）
│   ├── wechat_bot.py          # 基础微信机器人（基类）
│   ├── wx_backend/
│   │   ├── visual_backend.py  # 视觉后端（截图/OCR/气泡/头像/媒体检测）
│   │   └── __init__.py        # 后端协议 + 注册表 + create_backend
│   ├── xiaoli_app/
│   │   ├── config_store.py    # 配置加载/迁移/角色卡（含 UI 新字段默认值）
│   │   ├── engine.py          # 引擎线程 + 事件总线
│   │   └── ui/                # ★界面优化主战场
│   │       ├── __init__.py    # ★主题系统：THEMES(12套) + build_qss + 字体/壁纸库/缩略图
│   │       ├── backdrop.py    # ★粒子背景层（ParticleBackdrop：渐变+粒子+壁纸）
│   │       ├── main_window.py # 主窗口（左导航 + 右 QStackedWidget + 背景层集成）
│   │       ├── pages.py       # 六个页面（首页/角色卡/模型/任务/日志/设置）
│   │       ├── tray.py        # 托盘
│   │       └── _icons.py      # Lucide SVG 图标（内嵌）
│   ├── tests/                 # unittest 测试（225 用例）
│   └── requirements.txt
├── 壁纸/                      # 内置壁纸库（6 张，用户可自行增删；打包后复制进 dist\小漓\壁纸）
├── fonts/                     # 内置思源黑体 Noto Sans SC（Regular+Bold；打包后复制进 dist\小漓\fonts）
├── tools/                     # 一次性标定脚本
├── docs/                      # 调研文档 + 更新记录
├── 小漓.spec                  # PyInstaller 打包配置（gitignored 构建产物）
└── 交接文档.md                # 本文档
```

## 微信后端（已稳定，别乱动）

**核心不变量**：消息上边框 = 对应头像上边框（定位靠头像几何，不靠颜色）。改动后端前必读 `wx_backend/visual_backend.py` 的 `analyze_window`（约 1445 行）。已知坑（多轮实测沉淀）：会话名识别必须整窗 OCR；未读红圈用品牌红 #FA5151 像素检测；接收文件保留源时间戳，按显示名锚定；成果排除登记必须持久化。详见 `docs/` 与 git log。

## 本次会话做了什么（界面优化全记录）

提交链（按时间）：`ee4a23e` 主题系统重写 → `1b6df60` 粒子背景+缩略图选择器 → `86573ad` 壁纸库+默认壁纸 → `f1216e9` 深色黑底/黑边修复 → `8ca68e5` 透明度滑块+行号列修复+导航排满 → `6a289c6` 面板透明度+字号调大+文字美化 → `7be1ea4` 字号三档+透明度0~100%+清理失效设置 → `28ba882` 字号根因修复+思源黑体+aurora修复 → `881e505` 导航固定大字+表格列铺满 → `54ead23` 导航翻倍+弹窗大字+列宽比例。

用户反馈驱动的修复路径（下个 AI 参考）：
1. 「长条」= 旧 QSS 顶部光晕（qradialgradient）在浅色主题形成 180px 色带 → 背景改由 ParticleBackdrop 绘制
2. 「黑边/黑底」= 深色系统 palette 漏出：QDialog/QScrollArea viewport/QHeaderView 行号列 → QSS 显式覆盖 + 隐藏行号列
3. 「毛玻璃没效果」= QGroupBox 用实心 $frame → 改半透明 $card
4. 「字号调节没反应」= **Qt QSS 不解析无单位字号**（`font-size: 36` 无效，必须 `36px`）——_FONT_SCALES 曾缺 px 单位导致全档位失效
5. 「背景被剔除」= backdrop aurora 分支漏画壁纸 → 壁纸铺底提升为所有主题共用

## 当前 UI 架构（★下个 AI 必读）

### 主题系统（ui/__init__.py）
- `THEMES`：12 套主题（blue 兼容默认 + 11 套精选：fluent/m3/linear/aurora/ink/dracula/nord/onedark/catppuccin/cyberpunk/tokyonight）。每套 ~20 字段：label/group(浅色|深色)/bg/p1/p2/card(半透明rgba)/text/muted/border/hover/input_bg/frame/header/success/danger/warning/accent/focus/glow/scrollbar/card_alt。
- `_DEFAULTS`：主题缺字段兜底（新增主题不崩）。
- `build_qss(theme, wallpaper, card_opacity, panel_opacity, font_scale)`：按主题+壁纸+透明度+字号档位生成 QSS。动态派生 p1_hi/p2_hi（hover 提亮）、nav_hover、tip（text 80% 透明）。
- `_FONT_SCALES`：small/medium/large 三档字号（**必须带 px 单位**）。medium: base 15/nav 21/title 34；large: base 17/nav 26/title 42。注意：fs_nav 字段已弃用（QSS ::item font-size 不生效），导航字号由 main_window 的 _NAV_FONT setFont 固定 42px。
- `APP_QSS = build_qss()` 向后兼容（测试断言锚定：QComboBox QAbstractItemView 含 #374151、QTableWidget::item:selected 等）。

### 背景层（ui/backdrop.py）
- `ParticleBackdrop`：唯一背景绘制者（QSS 不再管背景）。paintEvent 三层：①壁纸铺底（所有主题共用，含 aurora）+遮罩 → ②主题渐变（aurora 斜向三段 / 其他径向光晕）→ ③粒子（glow 色系半透明圆点，30fps QTimer 漂移）。
- 深度主题粒子更亮（alpha 0.10-0.22）、浅色更淡（0.05-0.12）。
- **叠放方式**：main_window.py 用 `backdrop.lower()` + `central.resizeEvent` 手动同步几何（⚠ 不要改回 QStackedLayout——它隐藏非当前页导致背景层不渲染，这是已踩过的坑）。
- `set_theme(key)` 重建粒子、`set_wallpaper(path)` 切壁纸。

### 主窗口（ui/main_window.py）
- 左导航 `navList`：**垂直排布（图标上/文字下）+ 尺寸固定（不随字号档位变化）**。
  ⚠ 关键一：导航字号必须**代码 setFont（_NAV_FONT，pixelSize=38）**——Qt 不把
  QSS 的 `::item font-size` 应用到 item 渲染（item 继承 view 字体），QSS 写
  px 实际无效（实测 small/large 档下导航文字高度随 base 字号 13px→17px 变化，
  用户反复反馈「导航随字号变」的真根因）。
  ⚠ 关键二：**NavItemDelegate 自绘垂直排布**——QListWidgetItem 默认图标在左
  文字在右，横排（图标 48 + 42px×3 字）在 200px 导航必然截断（用户实测
  「角色卡→角···」）。delegate 画图标上/文字下，宽度只受图标与单字约束，
  任何导航名不截断；保留 QSS 选中渐变背景（super().paint 绘制底）+ 选中态
  左侧高亮竖条（现代侧边栏风格）。尺寸已按用户反馈 -10%（图标 48→43、
  字号 42→38）。
- `_layout_nav()`：6 项均分导航高度（最小 96px 容纳垂直排布，showEvent/resizeEvent 重算，防抖 flag）。
- `apply_backdrop(theme, wallpaper)`：主题/壁纸切换同步背景层 + 遍历页面调
  `apply_theme(theme)` 钩子（首页标题光晕随主题主色刷新）。

### 设置页（ui/pages.py SettingsPage）
- 主题缩略图网格（`render_theme_thumb` 手绘示意卡，点击即应用+保存，选中主色描边）
- 壁纸库网格（`render_wallpaper_thumb` QImageReader 按尺寸解码；扫描项目根「壁纸」目录；含「无壁纸」项 + 导入/清除按钮）
- 卡片透明度滑块（0~100%，默认 50%）+ 面板透明度滑块（0~100%，默认 50%，控制 input_bg 大白块）——两者独立
- 字号档位下拉（小/中/大，`font_scale`，默认 medium）
- 已删除失效项：成果文件发送方式、图片点击偏移（config 字段 image_click_offset 保留供后端 bot 读取）
- `_apply_theme_qss(theme, wp)`：统一入口，读 cfg 的 card_opacity/panel_opacity/font_scale 重建 QSS + 同步 backdrop

### 字体（ui/__init__.py load_app_font）
- 思源黑体 Noto Sans SC（fonts/ 目录，Regular+Bold），`QFontDatabase.addApplicationFont` 从文件加载（不依赖系统注册——DirectWrite 扫描用户字体目录有滞后）。
- QSS font-family 优先 "Noto Sans SC"，缺失回退微软雅黑。gui 入口在 setStyleSheet 前调用。

### 弹窗/确认框
- QMessageBox/QDialog **固定 18px**（不随字号档位）、内容 16px、按钮 16px+增高——用户要求弹窗始终清晰。

### 表格（模型页/任务页）
- 隐藏行号列（verticalHeader 不可见，防深色 palette 竖黑条）
- 模型页列宽：名称 90 / Base URL 240 / API Key 230 / **模型 Stretch**（拉满剩余）/ ID 70 固定短；`setWordWrap(False)`（单行截断防溢出下边框）
- 任务页：末列 stretch + 禁换行

### config 新增字段（config_store.py 默认值）
- `theme`（默认 "blue"）、`wallpaper_path`（默认空，首次启动兜底内置默认壁纸）
- `card_opacity`（0.5）、`panel_opacity`（0.5）、`font_scale`（"medium"）

## 已知遗留与待办（★下个 AI 接手点）

1. **用户级验收待确认**（最重要）：最新包已修复导航字号真根因（QSS ::item font-size 不生效 → 改 setFont 固定 42px）+ 导航图标 setIconSize 48px（此前 QListWidget 默认 16px 钳制，SVG 一直显示很小）+ 模型页 ID 列 70→110。待用户实测确认。
2. **dist_tmp1~8 清理**：8 个绕行打包目录（约 3GB+，`dist_tmp/`~`dist_tmp8/`）待清理（等用户确认包没问题后）。`dist\小漓\wxauto\202608170013363330` 空目录被系统层句柄锁（重启后可删）。
3. **毛玻璃真模糊**：当前是半透明近似（Qt QSS 无高斯模糊）。若用户要真亚克力，需自绘层方案（成本高，先问用户）。
4. **可能的优化方向**：设置页布局（双滑块+网格在滚动区内）、首页卡片布局、托盘菜单样式、更多壁纸/主题、字号档位细分（加超大档）。
5. **打包坑**：① `dist\小漓` 被运行中的小漓/微信进程锁 `_internal`/`wxauto` 时，PyInstaller rmtree 失败——打包前先 `Stop-Process -Name 小漓` + 微信关闭；锁残留可等几秒重试。② 打包后手动复制 `壁纸/` 和 `fonts/` 到 `dist\小漓\`（spec 未收集这两个目录）。③ 启动验证后必须杀净进程（PyInstaller one-dir 主进程被杀可能留子进程锁 _internal）。

## 测试与打包

**测试**：`cd D:\AI\小漓\xiaoli_desktop` 后 `..\.venv\Scripts\python.exe -m unittest tests.test_ui_smoke tests.test_config_store tests.test_setup tests.test_windowed_import tests.test_visual_backend tests.test_wechat_bot tests.test_task_attachment`（225 用例，约 30s）。UI 改动重点跑 test_ui_smoke（断言：QComboBox 下拉弹层 #374151、QTableWidget 选中态、设置页 ed_tasks 等）+ test_config_store（config 默认字段，含 image_click_offset/file_send_method 锚定——不要删这两个字段）。

**打包**：`cd D:\AI\小漓 && .venv\Scripts\python.exe -m PyInstaller --clean --noconfirm --distpath dist_tmpN 小漓.spec`（用 dist_tmpN 绕开 dist\小漓 锁定），然后复制 `壁纸/` `fonts/` 进产物，再覆盖 `dist\小漓\_internal` + `小漓.exe`。标准打包（微信关闭时）可直出 dist\小漓。

**视觉验证**：offscreen 渲染截图（参考 `.rivet/scratch/theme_shots.py`）+ PIL 像素分析（验证主题色应用/黑边/粒子/壁纸透出）。Qt 桌面应用不能用 browser_debug；read_file 拒读 PNG，用 PIL 数值验证。

## 给下个 AI 的接手指南

- 所有 UI 样式在 `xiaoli_app/ui/__init__.py` 的 `_QSS_TEMPLATE` + `THEMES`；布局在 `pages.py`/`main_window.py`；背景/粒子在 `backdrop.py`。改样式先读这三个文件。
- 新增主题 = THEMES 加条目（字段从 _DEFAULTS 补齐）；新增设置项 = pages.py 设置页 + config_store 默认 + _apply_theme_qss 接线。
- 字号/透明度/字体都是配置化的，用户反馈「太小/太透/不好看」→ 调参数即可，不要动结构。
- Qt QSS 三坑：字号必须带 px、data URI 不支持（装饰素材用运行时绘制或文件）、QStackedLayout 会隐藏背景层。
- 微信后端（wx_backend/wechat_bot.py/xiaoli_bot.py）已稳定，UI 优化**不要碰**。
- 用户偏好：导航要大要固定、弹窗要大、表格要铺满、喜欢透明毛玻璃和粒子动效、要好看的字体。
