# 交接文档：小漓微信视觉后端（第 5 轮交接）

> 本文档给下一个 AI 接续推进用。当前 HEAD=`63babd5`，302 项测试全绿，已重新打包 `dist\小漓\`（15:12 生成，含本轮全部改动）。
> 微信版本约束（延续，不得违反）：**不降级微信**，必须在 4.1.12.51 上完成。数据库解密路线已放弃。
> 唯一可用通道：visual（PrintWindow + RapidOCR + pyautogui）；wxauto4 结构性失效，勿依赖。

## 一、本轮工作（上一轮 `91f7e5f` 之后的所有改动，按 commit 时间，共 3 个 commit）

上一轮交接停在 `91f7e5f`（289 测试）。本轮解决三块：任务桥截断、界面美化、消息读取通道重构。

1. **任务桥多行气泡归并修复**（`85cdb25`）：
   - 根因（真机日志锁死）：长多镜头任务指令（含镜头1/2/3）被 `get_messages` 拆成 12 条独立消息，只有带 `@小漓` 的第一行触发任务，`task.json` raw_message 只剩「测试，生成10秒视频」。原因两个：y 阈值 18px 远小于气泡内换行距（2x 后约 44px，真机实测约 65px），每行都触发换块；且 `if not cur_lines:` 无 else 分支，续行被静默丢弃。
   - 修复：`_BUBBLE_LINE_GAP = 70`（夹在换行距 44~65 和气泡间距 ≥106 之间）+ 补续行 append 分支。

2. **界面美化**（`d0bb0c5`）：
   - 标题「小漓」用 `qlineargradient` 渐变文字，Windows ClearType 亚像素渲染产生红绿彩色描边（用户反馈"文字阴影难看"）。修复：标题改纯色 `$p1` + `xiaoli_gui.py` 全局字体 `PreferAntialias`（灰度抗锯齿，消除 subpixel fringe）。
   - 左侧导航布局：`main_window.py` 从 QTabWidget 改成 QListWidget 导航 + QStackedWidget 内容，6 个导航项带 Lucide SVG 图标（ISC 协议，下载自 unpkg，内嵌进 `_icons.py` 字符串——打包零配置，不依赖 gitignored 的 spec datas）。

3. **消息读取通道重构**（`63babd5`，本轮最大的一块）：
   - `[动画表情]` 识别：`_PREVIEW_TYPE_PATTERNS` 表情正则从 `^\[\s*表情\s*\]` 改为 `^\[\s*(动画)?表情\s*\]`（微信列表预览实际是 `[动画表情]`，旧正则匹配不上 → EMOJI 静默降级 TEXT）。
   - 群聊过滤表情：`process_new_messages` 加「群聊 EMOJI 直接跳过」，for 循环 + latest 兜底两处都加（见踩坑 2）。
   - 图片精确点击截图：新增 `find_media_boxes`（检测非背景非气泡的媒体矩形）+ `media_screen_boxes`（换算屏幕坐标），`_process_image` 的 visual 路径从整屏截图降级改成「点击图片中心 → 截预览窗 → ESC 关闭」。
   - 头像文字排除：`_detect_self_avatar_boxes`（自己头像模板匹配返回矩形）+ 对方头像几何反推（气泡框左边缘左侧那一列），气泡外且落在头像区域的 OCR 文字直接丢弃。
   - `detect_bubble_colors` 的 other（对方气泡色）探测收窄成「与背景接近但不同」的像素（总色阶差 10~90），防止大块图片污染中位数。

## 二、关键代码位置（file:line 为本轮核实，改动后需重查）

| 位置 | 作用 |
|---|---|
| `visual_backend.py:280` `detect_bubble_colors` | 自动探测背景/对方气泡/自己气泡色（other 收窄 10~90 防图片污染） |
| `visual_backend.py:361` `find_bubble_boxes` | 颜色连通域框气泡（深灰/绿色），过滤 >60% 高的异常 other 框 |
| `visual_backend.py:386` `find_media_boxes` | 检测图片/视频/表情内容矩形（非背景非气泡大块） |
| `visual_backend.py:534` `_BUBBLE_LINE_GAP = 70` | 气泡内换行 vs 气泡间距的 y 阈值 |
| `visual_backend.py:625` `_PREVIEW_TYPE_PATTERNS` | 列表预览类型识别（含 `[动画表情]`） |
| `visual_backend.py:1012` `_detect_self_avatar_ys` | 自己头像 y 坐标（sender 判定用） |
| `visual_backend.py:1045` `_detect_self_avatar_boxes` | 自己头像矩形（头像文字排除用） |
| `visual_backend.py:1079` `get_messages` | 消息读取主流程：气泡归属 + 头像文字排除 + 行合并 |
| `visual_backend.py:1399` `media_screen_boxes` | 媒体矩形 → 屏幕坐标（图片点击用） |
| `xiaoli_bot.py:1071` `process_new_messages` | AgentBot 覆写版主循环（群聊 EMOJI 过滤在 1137 + 1211） |
| `wechat_bot.py:480` `_process_image` | 图片处理（visual 路径 511 用 media_screen_boxes 定位） |
| `wechat_bot.py:572` `_process_image_visual_fallback` | 整屏截图降级（现在仅 media 检测不到时回退） |
| `xiaoli_gui.py` `main()` | 全局字体 PreferAntialias + 主题恢复 |
| `ui/main_window.py` `_load_nav_icon` | 左侧导航图标（内嵌 SVG 运行时着色） |
| `ui/_icons.py` `SVG_ICONS` | 6 个 Lucide SVG 内嵌字符串（ISC 协议） |

## 三、遗留问题（下一个 AI 的工作，按优先级）

1. **【最高优先】图片精确点击截图未真机验证**：`media_screen_boxes` + `_process_image` visual 路径只有单元测试（mock 坐标），没真机点过图片。首次收到图片消息需实测点击定位准不准——不准看日志「点击图片: (x,y)」坐标，校准 `image_click_offset` 或 media 矩形检测。
2. **浅色主题气泡色探测未验证**：`detect_bubble_colors` 的 other 阈值（总色阶差 10~90）按深色主题实测推的（深灰 51、图片 309），浅色主题白气泡约 45 也在区间内，但没真机验证。切浅色主题后对方气泡识别异常，优先怀疑这个区间。
3. **头像文字排除依赖头像模板**：`_detect_self_avatar_boxes` 依赖 `avatar_template`（首启上传）。真机探针 `create_backend("visual")` 不传模板 → 匹配失败、头像文字只被隔离成独立消息而非完全排除。实际运行（`wechat_bot.py:333` `create_backend("auto", avatar_template=...)`）会传，需真机确认首启头像传对。
4. **打包已清空 dist 数据**：本次已打包（15:12），`cards\`/`config.json`（含 API key）/`memory.json`/`wxauto\` 已清空，需重新走首启引导填 key、选头像、圈区域。
5. **`_process_file_with_task` 死代码**：`xiaoli_bot.py:882` 定义了完整任务投递逻辑但 grep 无调用者（疑似被 `_process_file_with_instruction` 取代后未删），建议核实后删除或补回调用点。
6. **上一轮延续未解决**：普通用户分发（三区域相对比例绑 1300x1610，普通用户窗口错位）；取消选中根治（`_switch_chat` 靠进程内 `_current_chat`，重启清空多一次点击）；bot.log 打包路径（`_internal\bot.log` 首页能否显示未实测）；旧角色卡不自动更新（`migrate_config` 幂等）。

## 四、踩坑教训（下一个 AI 务必记住）

1. **RapidOCR 检测模型 `limit_side_len=736`（min）**（延续）：>736 宽的图被内部压缩，长文本行检测框不完整。消息区 2x 后 1508px 必超，必须 `ocr_image_sharded` 分片。
2. **latest 兜底路径漏 EMOJI**（本轮新）：`process_new_messages` for 循环里 `continue` 掉某类型后，`last_foreign` 仍是 None，循环后的 latest 兜底路径会把 `msgs[-1]` 重新捡起来处理。过滤某类型必须 for 循环 + latest 兜底两处都加（群聊 EMOJI 就是这么修的两处）。
3. **other 探测被大块图片污染**（本轮新）：`detect_bubble_colors` 用「非背景非绿色像素的中位数」探测对方气泡色，大块图片会把中位数拉成图片色（真机实测 other 变紫色 120,80,200）。修复是收窄成「与背景接近但不同」（总色阶差 10~90）。
4. **气泡内 8 字文本误判发送者名**（本轮新）：发送者名识别用 `len(text)<=8`，但「镜头1：缓慢推镜」恰好 8 字被误判。修复：气泡框内（`_bubble` 非 None）的短文本是内容不是发送者名，只有气泡外的短文本才走发送者名识别。
5. **气泡内换行行距真机约 65px（2x）**：阈值 70 夹在换行（44~65）和气泡间距（≥106）之间。若真机仍拆散/误合并，先查这个阈值和 `detect_bubble_colors` 是否探测失败（失败时回退 y 阈值）。
6. **终端窗口激活**（延续）：uiautomation `SetActive()` 不够，必须补 `SetForegroundWindow`。
7. **`send_trigger_to_window` 的 hold 语义**（延续）：CLI 刚启动 TUI 未就绪，hold 都要 2.0s。
8. **交付门禁 adopt + force**（延续）：多会话共享工作区，deliver_task 提交要 `adopt` 认领历史 owned 文件；跨区域提交触发内聚门禁需 `force=true`。
9. **打包必清空 dist\小漓**（延续）：每次打包前想清楚用户数据要不要备份（本轮用户明确说不备份）。
10. **中文发送必须剪贴板**（延续）：`typewrite` 对中文无效，必须 `pyperclip` + Ctrl+V。
11. **`process_new_messages` 基类 + AgentBot 覆写两版**（延续）：真机走 AgentBot，写测试必须建 AgentBot 实例。

## 五、测试 / 打包 / 运行

- **测试**：`cd D:\AI\小漓 && .venv\Scripts\python.exe -m unittest discover -s xiaoli_desktop\tests -p "test_*.py"`（当前 302 项全绿）。
- **真机读消息**：`.venv\Scripts\python.exe tools\test_read_messages.py 强盗”集团`（读会话消息打印 sender/type/content）。
- **真机气泡/媒体探针**：`.venv\Scripts\python.exe .rivet\scratch\bubble_probe.py`（截图 + OCR + 气泡归属标记，输出到 `.rivet\scratch\bubble_probe\`）。
- **框选区域**：`.venv\Scripts\python.exe tools\pick_ocr_region.py`（拖三框：列表区→消息区→标题区；右键撤销；45s 超时）。
- **打包**：`.venv\Scripts\python.exe -m PyInstaller 小漓.spec --noconfirm` → `dist\小漓\`（spec 已带 `icon='小漓.ico'`）。
- **运行**：`dist\小漓\小漓.exe`；日志 `dist\小漓\_internal\bot.log`。

## 六、环境依赖

- Python 3.12.9，venv `D:\AI\小漓\.venv`。
- 关键依赖：`rapidocr-onnxruntime==1.4.4`、`onnxruntime`、`opencv-python`、`numpy`、`PySide6`（6.11.1，含 QtSvg）、`pyautogui`、`pyperclip`、`uiautomation`、`Pillow`、`wxauto4`（仅降级死路，勿依赖）。
- PyInstaller 6.21.0。
- QtSvg **已装**（PySide6 6.11.1 自带，上一轮"未装"是旧状态）——SVG 图标已落地（内嵌字符串，无需 spec datas）。

## 七、不要做的事

- 不要降级微信、不要回头走数据库解密路线、不要用 Windows OCR（winsdk）。
- 不要依赖 wxauto4（4.1.12 结构性失效）。
- 不要用像素亮度判断「黑屏」（深色主题会误判）。
- **实机探针不要 force 连点同一会话**（toggle 取消选中，读空是自造的）。
- 不要擅自删除用户机器上的 `cards\xiaoli.json`（破坏性操作，需确认）。
- 不要硬编码气泡色/背景色——已改成 `detect_bubble_colors` 自动探测，深浅主题都走探测。
