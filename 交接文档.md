# 交接文档：小漓新版微信高效通道验证与微信层重实现

> 本文档为**下一个 AI 接续任务**的交接说明。
> 交接时间：2026-08-09（第二次更新）。本次更新者：天梁域 AI（天枢）。
> 上一份交接（破军域）覆盖 Wave 0-2；本份追加 Wave 3 实测结论与后续遗留。

---

## 1. 任务背景（一句话）

新版微信 4.1.12.51 的 UI 从 Qt 原生控件切到 Chromium 渲染，**wxauto4 全部失效**（UIA 控件树结构性关闭），需要在"小漓"微信机器人（xiaoli_desktop\wechat_bot.py）上恢复微信消息收发能力，且用户明确否决"每轮调用多模态 AI 识别截图"的低效方案，要求像 wxauto4 那样高效。

## 2. 计划状态

- `.rivet\plans\小漓新版微信高效通道验证与微信层重实现.md`：APPROVED，Wave 0-2 完成，**Wave 3（真实联调）已由本次会话完成**，未 plan close
- `.rivet\plans\小漓微信新消息获取重构-未读红圈角标驱动-双区域分开-ocr.md`：EXECUTED（commit 476697b）
- `.rivet\plans\小漓-ocr-区域用户圈定-图形框选-手填坐标.md`：EXECUTED（commit 00e1d1f）
- `.rivet\plans\pick-ocr-region-改全屏框选-窗口-rect-双-ocr-区域一次圈定.md`：EXECUTED（commit 7c63b47）

**已提交 commit（本次会话 5 个）**：476697b（红圈驱动）→ 00e1d1f（区域圈定）→ 9ed075f（DPI 修复）→ 7c63b47（window_rect）。

## 3. 关键决策记录（为什么这么做）

### 3.1 通道选型（Wave 0 实测，本次会话再次验证）

所有"高效通道"在新版微信 4.1.12.51 上**全部关闭**，唯一可用通道是 **PrintWindow 截图 + 本地 OCR**：

| 通道 | 判定 | 证据 |
|---|---|---|
| UIA 控件树 | 失败 | **本次会话实测复现**：ControlFromHandle 拿到 WindowControl('微信')，但后代控件仅 2 个（一个 PaneControl 'Weixin'），会话列表/消息区/输入框全部不在树内——wxauto4 失效根因 |
| CDP 远程调试 | 失败 | 参数进命令行但无 DevToolsActivePort、端口不可达（微信禁用 CDP） |
| 窗口消息模拟 | 不可行 | EnumChildWindows 主窗口零子窗口（内容全在 Chromium 渲染层） |
| 本地消息数据 | 不可读 | 消息存储为加密 mmkv；login\wxid 仅 key_info.dat |
| **PrintWindow 截图 + OCR** | **可行（唯一）** | 本次会话真实未读场景全链路验证通过（见 §13.3） |

完整证据见 `docs\微信通道验证结论.md`。

### 3.2 效率方案（回应用户诉求）

用户的"高效"诉求通过三层实现，**不是**每轮调多模态 AI：
1. **本地 OCR 替代多模态**：文本消息用 Windows 自带 OCR（winsdk，zh-Hans-CN，毫秒级）
2. **像素变化检测先行**：截图后先做区域 diff，无变化直接跳过（注意：仅用于**变化检测**，不能用于"当前是否有角标"的状态检测——见 §10.3）
3. **仅图片消息**才由上层调多模态 AI 描述

实测（本次会话）：connect 0.008-0.03s、iter_unread_sessions ~0.35s（含截图+红簇检测+整窗 OCR）、get_messages ~0.9s（点击+消息区 OCR）。

### 3.3 窗口位置纪律（重要！用户明确要求）

- **bot 绝不移动微信窗口**——窗口位置/大小是用户手动控制的资产
- visual_backend 的 connect() **没有** SetWindowPos 逻辑
- **wxauto 后端有隐藏副作用**：`WeChat()` 实例化会触发微信窗口恢复到记忆位置 → visual 必须排在 auto 链最前（priority=1），wxauto 降级（priority=100）
- 窗口固定工具：`tools\fix_window.py`（配置存 `xiaoli_desktop\wx_window.json`，bot 运行时**不读取**）

## 4. 已提交改动（全部在 git 历史，本次会话 commit 详见 §2）

| 文件 | 说明 |
|---|---|
| `xiaoli_desktop\wx_backend\__init__.py` | 抽象层：WeChatBackend Protocol + 注册表 + auto 降级（visual priority=1 / wxauto priority=100）；docstring 标注 iter_unread_sessions 为可选扩展（不在方法集，isinstance 不受影响） |
| `xiaoli_desktop\wx_backend\models.py` | 统一消息模型 WeChatMessage / MessageType（text/image/file/time/system） |
| `xiaoli_desktop\wx_backend\visual_backend.py` | 核心后端：PrintWindow 截图 + winsdk OCR + 红圈角标检测 + 区域配置加载 + DPI 感知声明（见 §13.2） |
| `xiaoli_desktop\wx_backend\wxauto_backend.py` | 包装 wxauto4 的旧版兼容后端（priority=100 降级项） |
| `xiaoli_desktop\wechat_bot.py` | process_new_messages 改 hasattr 降级驱动：有 iter_unread_sessions → 只处理红圈会话；无 → 走旧 iter_sessions 全量路径 |
| `xiaoli_desktop\tests\test_visual_backend.py` | 35 用例（红圈检测/配置加载/消息切分/发送） |
| `xiaoli_desktop\tests\test_wechat_bot.py` | 26 用例；修复测试自身缺陷（finally 恢复对象错位） |
| `tools\pick_ocr_region.py` | 区域圈定工具：PrintWindow 截图作背景 + 双区域拖框 + 窗口 rect 自动记录 + 手填/--read/--clear |
| `tools\fix_window.py` / `tools\cdp_probe.py` | 窗口固定工具 / CDP 注入探针（Wave 0） |

**测试现状（全部通过）**：visual 35 + wx_backend 39 + wechat_bot 24 + task_pause + task_attachment + config_store + engine + card_store（9 模块全绿）。
```bash
.venv\Scripts\python.exe -m unittest xiaoli_desktop.tests.test_visual_backend
.venv\Scripts\python.exe -m unittest xiaoli_desktop.tests.test_wx_backend
.venv\Scripts\python.exe -m unittest xiaoli_desktop.tests.test_wechat_bot
```
注意：`python -m unittest discover` 报 `__file__` None（包结构问题），**逐个模块跑**。

## 5. 真实微信联调已验证（本次会话新增，Wave 3 核心）

**完整新消息链路（真实未读场景，用户用手机给小漓发消息）**：
```
connect: 0.029s
iter_unread_sessions: 0.353s → ['王文生']   # 红圈角标被检测，正确识别未读会话
get_messages(王文生): 0.944s → 3 条         # 点击切换 + 消息区 OCR 读出消息
```
- 红圈角标像素检测（#FA5151 阈值）在真实窗口**工作正常**，位置在联系人名字右侧
- 用户圈定的区域配置（wx_ocr_region.json）被 visual_backend 正确加载生效
- 会话识别 4 个：王文生 / 杨冬梅 / 王美晨 / 「强盗」集团（与真实会话一致）

## 6. 遗留问题（下一个 AI 必须知道，按优先级）

### 6.1 高优先级：消息类型判定未实现（新发现，用户明确关注）

**现状**：`get_messages` 把所有消息都标成 `MessageType.TEXT`——**无法区分对方发的是文字/图片/文件消息**。这直接影响 bot 收到图片时走 `_process_image`（类型识别缺失会失效）。

**用户确认的关键 UI 事实（本次会话反复实测）**：
- 文字消息：**有气泡**——小漓发的绿色气泡（~#2BA245 系）、对方发的灰黑色气泡（深色模式下，不是白色）
- **图片/视频/文件消息：没有气泡**！是独立渲染的卡片（图片缩略图 / 视频封面+播放按钮 / 文件卡片），OCR 读不出它们本身
- 实测：get_messages 读到的是视频卡片上的**文字说明**（"秒剑客立于满月之下…""请查收附件"），被误当成独立文本消息——视频/图片本身完全没被识别

**判定方向（待实现，需真实样本验证）**：消息块"是否有气泡底色"是核心判别信号——有气泡→文字；无气泡但有图像内容→图片/视频/文件。用像素检测气泡颜色（绿色/灰黑）区分 sender 与 text，用"无气泡+内容块"判定 image/file。**这条信号链需要用户发一张真实图片/文件后采样验证**（当前仅有视频卡片样本）。

### 6.2 中优先级（既有，未修）

1. **OCR 会话名残留**：`艹王美晨` 前缀"艹"、`强盗"集团` 引号内空格——iter_sessions 有清理但不够彻底
2. **消息切分质量**：get_messages 仍有噪音/切分不准（`；eel< 氵巛` 这类 OCR 噪音进消息、群聊预览混入）
3. **send_file 未实现**：visual_backend.send_file 返回 False
4. **locate_message 返回 None**：视觉模式无法精确定位单条消息（图片处理走整窗截图降级）
5. **visual 消息 id `visual_{seq}` 跨轮不稳定**：新消息导致列表偏移时旧消息可能获得新 id，去重失效（cooldown 兜底）

### 6.3 未验证项

- **真实发送验证**：send_text 真实发一条消息确认坐标点击+键盘输入+Enter 链路（只验证过读取方向）
- **图片消息端到端**：_process_image_visual_fallback（整窗截图送视觉模型）需真实图片验证
- **requirements.txt**：winsdk==1.0.0b10 已装在根 .venv，未写进 requirements（见 §9 环境勘误）
- **多屏场景**：当前微信在主屏 (726,0)，副屏场景未验证

## 7. 下一步建议（按优先级）

1. **实现消息类型判定**（6.1）——用户核心关注：气泡检测（text）vs 无气泡内容块（image/file）。先让用户发真实图片+文件采样，验证像素特征再实现
2. **修 OCR 会话名残留**（6.2.1）——影响 is_group_chat 判断与切换点击
3. **真实发送验证**（6.3）——send_text 链路
4. **补 winsdk 进 requirements.txt**
5. **Wave 0-2 计划 plan close**（`.rivet\plans\小漓新版微信高效通道验证与微信层重实现.md`）

## 8. 关键技术事实（避免重新踩坑）

- **微信版本**：4.1.12.51（升级风险是常态，架构解耦就是为了抗这个）
- **微信安装**：D:\腾讯\Weixin\Weixin.exe，主进程裸启动无调试端口，子进程全 Chromium mojo
- **OCR 引擎**：winsdk（Windows 自带，zh-Hans-CN 可用）；**对整窗识别最准，对裁剪放大区域反而差**（'王文生'放大 2 倍被读成'干立牛'；列表区 crop 无论原尺寸/放大 2x/反色都只读"Q 搜索"）——会话名识别必须整窗 OCR
- **截图**：PrintWindow + PW_RENDERFULLCONTENT 能抓非前台窗口（抗遮挡）；需处理 64 位句柄（ctypes 设 argtypes/restype）
- **DPI 感知**：visual_backend 模块导入时 SetProcessDpiAwareness(2)——不声明则 GetWindowRect 返回虚拟化逻辑尺寸（743×920），截图只渲染窗口上部 62%，表现为"不是整个微信窗口"；声明后返回物理尺寸（1300×1610），截图 100% 完整（详见 §13.2）
- **深色主题**：Windows OCR 对深色背景读 0 行，ocr_image 内置平均亮度 <100 反色自适应；但像素检测（红圈/气泡）在反色前原图做，不受影响
- **测试技巧**：wxauto4 消息类有抽象方法+构造参数，测试用 `cls.__new__` + 清空 `__abstractmethods__` 绕过

## 9. 环境（勘误后）

- Python 3.12.9，**根 .venv 在 D:\AI\小漓\.venv**（⚠️ 交接 v1 说的 xiaoli_desktop\.venv **没有 winsdk**——依赖装在根 .venv，测试/联调统一用根 `.venv\Scripts\python.exe`）
- 已装 winsdk==1.0.0b10（requirements 未更新）、PySide6==6.11.1（图形框选用）
- 微信 4.1.12.51 登录账号"小漓"，窗口用户手动固定在右半边，**物理尺寸 1300×1610，位置 (1271,0)**

---

## 10. 2026-08-09 追加：未读红圈角标驱动 + 双区域分开 OCR（已完成）

### 10.1 做了什么

重构"获取最新消息"链路：**未读红圈角标驱动**，双区域分开读。

- `visual_backend.py`：`_detect_red_clusters`（列表区 crop + PIL point 掩码，~毫秒级无 OCR）+ `iter_unread_sessions`（红圈驱动）+ `_extract_session_names`（会话名提取，供 iter_sessions/iter_unread_sessions 复用）
- `wechat_bot.py`：process_new_messages 改 hasattr 降级驱动
- 测试 +8 用例；修复 test_wechat_bot.py 测试自身缺陷

### 10.2 实测数字（真实微信）

- connect 0.008-0.03s；iter_unread_sessions 0.215-0.353s（含红圈检测+整窗 OCR）；get_messages 0.925-0.944s
- 无未读时：仅截图+像素检测（~0.012s，零 OCR 零点击）

### 10.3 关键实测结论（避免踩坑）

- **红圈可检测**：微信品牌红 #FA5151，阈值 r>=200, g<=130, b<=130 有效；位置在联系人名字右侧（⚠️ 交接 v1 说的"左上方"是逻辑坐标下的误读，物理坐标下实测在名字右侧——匹配规则已用 |dy|<60 且红簇 x 与名字的关系，实测可用）
- **iter_unread_sessions 必须 force=True 截图**：不能用 region_changed 跳过——diff 检测"相对上一帧的变化"，红圈已存在时两帧相同会误判"无变化"漏读；diff 的价值在于"无红簇时跳过 OCR"，由红簇检测结果承担

### 10.4 遗留

- 消息切分噪音（6.2.2）；红圈未匹配会话名只记 warning 不阻断；visual 消息 id 跨轮不稳定（6.2.5）

---

## 11. 2026-08-09 追加：OCR 区域用户圈定（已完成）

### 11.1 做了什么

把两个 OCR 区域从硬编码常量改为**用户可圈定**（图形框选 + 手填坐标）。

- `tools\pick_ocr_region.py`（新增）：图形框选 + `--session/--message` 手填 + `--read/--clear`
- `visual_backend.py`：`_load_region_config`（读+校验+fail-closed 回退）+ `_normalize_region` + `_region_dict_to_tuple`；`__init__` 加载实例区域；4 处引用替换（红簇检测/消息区 crop/输入框/session_x_max）
- 测试 +8 用例

### 11.2 用法

```bash
python tools/pick_ocr_region.py                      # 图形框选
python tools/pick_ocr_region.py --session 0 0.09 0.42 1.0    # 手填列表区
python tools/pick_ocr_region.py --message 0.42 0.08 1.0 0.84 # 手填消息区
python tools/pick_ocr_region.py --read / --clear
```

配置存 `xiaoli_desktop\wx_ocr_region.json`，**重启 bot 后生效**。无配置/坏配置一律回退默认常量，bot 不中断。

### 11.3 实测结论

- 配置格式兼容 dict 与 list；fail-closed 验证（坏 json/l>=r/越界/缺文件 → 回退默认 + WARNING 日志）
- **用户已实际圈定**：当前配置 session=[0.09,0.0855,0.4165,0.993]、message=[0.4151,0.0843,0.9913,0.8372]、window_rect=(1271,0) 1300×1610

---

## 12. 2026-08-09 追加：pick_ocr_region 记录 window_rect（已完成）

- 工具截图源保留 **PrintWindow**（抗遮挡，微信不在最前也能读完整内容）
- 图形框选自动记录微信窗口屏幕 rect（GetWindowRect，DPI aware 物理坐标），配置存 window_rect 键
- `_load_region_config` 用 data.get 只取双区域键 → window_rect 多余键零改动容忍（+1 测试锁定）
- 运行时视觉后端仍实时取微信当前 rect 换算比例 → 窗口移动后区域自适应

**⚠️ §12.4 勘误（交接 v1 错误结论）**：v1 声称"未读角标是绿色圆 (13,168,105)，红色阈值检测不到"。**这是误判**——当时测到的大片绿色 (13,168,105) 是**选中行背景**，不是角标。用户实测 + 本次会话真实未读场景验证：**角标是红色 #FA5151，红圈检测工作正常**（见 §5/§13.3）。不需要改成绿色检测，不要被 v1 误导。

---

## 13. 2026-08-09 追加：本次会话完整成果与结论（天梁域）

### 13.1 已实现功能全景

1. **未读检测**：红圈角标像素检测 → 只处理有未读的会话（iter_unread_sessions）
2. **区域圈定**：用户图形框选/手填窗口位置 + 双 OCR 区域，配置驱动，fail-closed
3. **消息读取**：红圈会话 → 点击切换 → 消息区 OCR → 时间戳切块 → x 中线分 sender
4. **全链路真实验证通过**（§5）

### 13.2 DPI 感知修复（本次会话最重要的底层修复）

**症状**：图形框选弹窗只显示微信窗口上半部分；截图内容占比仅 62%。
**根因**：进程未声明 DPI 感知（unaware），GetWindowRect 返回被系统虚拟化的逻辑尺寸 743×920，而微信真实物理窗口是 1300×1610——capture_window 按逻辑尺寸建位图，PrintWindow 只渲染出上部 62%。
**修复**：visual_backend 模块导入时 `SetProcessDpiAwareness(2)`（PROCESS_PER_MONITOR_DPI_AWARE），之后 GetWindowRect 返回物理坐标，截图 100% 完整，坐标换算（pyautogui 点击）也更准。
**影响**：所有窗口坐标从逻辑变物理——visual_backend 内部坐标换算均基于 GetWindowRect 实时值，语义不变，无需额外适配。测试用 mock 合成图不受影响。

### 13.3 关键验证记录（真实微信 4.1.12.51，用户手机发消息触发未读）

```
iter_unread_sessions: 0.353s → ['王文生']   # ✅ 红圈角标检测 + 会话名关联
get_messages(王文生): 0.944s → 3 条         # ✅ 点击切换 + 消息区 OCR
```
UI 树实测：ControlFromHandle 拿到外层 WindowControl('微信')，后代仅 1 个 PaneControl，**会话内容全部不在 UIA 树**——视觉方案是唯一通道。

### 13.4 消息区 UI 事实（用户确认，影响后续消息类型判定）

- 深色模式下：小漓发的文字消息 = **绿色气泡**；对方文字消息 = **灰黑色气泡**
- **图片/视频/文件消息 = 无气泡**，独立卡片渲染（缩略图/播放按钮/文件卡片）
- OCR 读不出图片/视频本身，只读出卡片上的文字说明（会被误当成独立文本消息）
- 详见 §6.1 的消息类型判定待实现项

### 13.5 已提交 commit 清单

| commit | 内容 |
|---|---|
| 476697b | 红圈角标驱动 + 双区域分开 OCR |
| 00e1d1f | OCR 区域用户圈定（pick_ocr_region + 配置加载） |
| 9ed075f | DPI 修复（图形框选高 DPI 只显示部分窗口） |
| 7c63b47 | pick_ocr_region 记录 window_rect（三要素一次圈定） |

### 13.6 工作区未提交项（属 Wave 0-2 遗留，非本次会话）

`docs\微信通道验证结论.md`、`tools\fix_window.py`、`tools\cdp_probe.py`、`wxauto_new_feasibility.md`、`xiaoli_desktop\tests\test_wx_backend.py`、`test_wxauto_backend.py`、`xiaoli_desktop\wx_backend\models.py`、`wxauto_backend.py`、`xiaoli_desktop\wx_window.json`、`wx_ocr_region.json`（运行时配置）、`交接文档.md`（本文件）——均为 untracked，下一个 AI 可按需交付。
