# 小漓 交接文档

> 状态：微信消息链路已实测跑通，下一步做前端美化。本文档供下一个 AI 无缝接手。

## 一句话现状

小漓是 Windows 微信 AI 机器人桌面版（PySide6 GUI + 视觉后端自动化微信 + LLM 聊天 + 天枢任务桥）。本次会话完成了微信「消息定位」的重构修复，已实机验证「文字+图片+文字」连续消息能正确识别、不再误判文件或判窗口空。**当前唯一待办方向是前端美化**，微信后端逻辑稳定，美化时不要动 `wx_backend`。

## 项目结构速览

```
小漓/
├── xiaoli_desktop/
│   ├── xiaoli_gui.py          # GUI 入口（PySide6，托盘常驻）
│   ├── xiaoli_bot.py          # 合并版 bot：聊天 + 天枢任务桥（实际运行的是它）
│   ├── wechat_bot.py          # 基础微信机器人（基类）
│   ├── wx_backend/
│   │   ├── visual_backend.py  # 视觉后端（截图/OCR/气泡/头像/媒体检测）★本次重构核心
│   │   └── __init__.py        # 后端协议 + 注册表 + create_backend
│   ├── xiaoli_app/
│   │   ├── config_store.py    # 配置加载/迁移/角色卡
│   │   ├── engine.py          # 引擎线程 + 事件总线
│   │   └── ui/                # ★前端美化主战场
│   │       ├── __init__.py    # 主题系统：THEMES(5套配色) + build_qss + 毛玻璃
│   │       ├── main_window.py # 主窗口（左导航 200px + 右 QStackedWidget）
│   │       ├── pages.py       # 六个页面（63KB，最大）
│   │       ├── tray.py        # 托盘
│   │       └── _icons.py      # Lucide SVG 图标（内嵌）
│   ├── tests/                 # unittest 测试
│   └── requirements.txt
├── tools/                     # 一次性标定脚本（fix_window / pick_ocr_region 等）
├── docs/                      # 调研文档 + 更新记录
├── 小漓.spec                  # PyInstaller 打包配置（入口 xiaoli_desktop/xiaoli_gui.py）
└── 交接文档.md                # 本文档
```

## 本次会话做了什么（微信消息定位重构）

**问题**：王文生给 bot 发「文件 → bot 回复"文件已收到" → 又发文字+图片+文字」，bot 把文件卡片误判成对方消息，且后面连续消息被判「窗口空」跳过。

**根因**：消息归属原来靠「颜色判据」——`find_bubble_boxes`/`find_media_boxes` 用气泡色/背景色阈值找消息框。深色主题下两个阈值都有缺陷：

- `find_media_boxes` 的 bg 阈值 30 太大，把黑色图片 `(15,15,17)`（离背景 `(30,30,31)` 只差 15）当背景吞掉，图片只检出中间一小段。
- `detect_bubble_colors` 的 other_mask 把比背景暗的深色图片也圈进"对方气泡色"，把气泡色中位数从 `(47,47,48)` 拉低成 `(39,39,41)`，接近背景后气泡检测把背景也当气泡吞了。

**修复**：改用一条硬不变量——**消息上边框 = 对应头像上边框**（头像几何比颜色可靠）。改了 4 处：

1. `detect_bubble_colors`：other_mask 加「比背景亮」约束，深色图片不再污染气泡色探测。
2. `find_media_boxes`：bg 阈值 30 → 8，深色内容不再被当背景排除。
3. `analyze_window`：`bot_bottom` 改纯几何（「我方最后头像之后第一条消息的上边框」，不再取 bot 气泡 bottom）；新增 `has_other` 字段（bot 之后有没有对方新消息，纯头像几何判据）；`other_text`/`other_media` 对齐「对方新消息头像」而非依赖颜色框。
4. `xiaoli_bot.py` / `wechat_bot.py`：判空条件加 `has_other`，气泡/media 检测漏了也有几何兜底。

**关键文件与位置**（`xiaoli_desktop\wx_backend\visual_backend.py`）：

- `detect_bubble_colors`（约 332 行）：气泡色探测
- `find_bubble_boxes`（约 413 行）/ `find_media_boxes`（约 444 行）：气泡/媒体框检测
- `detect_avatar_tops`（约 465 行）：头像顶部 y 检测（几何判据）
- `analyze_window`（约 1445 行）：窗口结构分析，本次重构核心
- `get_messages`（约 1083 行）：OCR 读消息 + sender 判定（头像几何优先于气泡色）

## 核心不变量（改微信后端前必读）

1. **消息上边框 = 对应头像上边框**。定位消息靠头像几何，不靠颜色。这是本次重构的根基，破坏它消息定位全崩。
2. 头像位置固定：右侧窄带 `x/w∈[0.84,0.98]` 是 bot 头像，左侧 `x/w∈[0.02,0.14]` 是对方头像。头像高 ≈ 消息区高/18。
3. 深色主题色值：背景 `(30,30,31)`、对方气泡 `(47,47,48)`、自己绿气泡 `(53,210,141)`。浅色主题对方气泡是白色。
4. `analyze_window` 返回 dict 有 7 个字段：`bot_bottom`（几何，我方最后回复之后第一条消息上边框）、`other_text`、`other_media`、`has_text`、`has_media`、`has_other`（几何判据）、`width`、`height`。

## 已知坑（微信视觉后端，别重踩）

这些是从多轮实测沉淀的教训，前端美化不碰它们，但了解能避免误改：

- 微信列表区 crop OCR 读不出会话名，会话名识别必须整窗 OCR。
- 未读红圈用像素检测（品牌红 `#FA5151`，阈值 r≥200/g≤130/b≤130），不能用「相对上一帧 diff」判断当前状态（红圈已存在时两帧相同会误判无变化）。
- 微信 PC 版会把 bot 通过剪贴板发送的文件复制进接收目录，是「成果被误当附件」的根源；成果排除登记必须持久化到盘，否则 bot 重启后排除失效。
- 微信 4.0 接收文件保留源时间戳，按 mtime/ctime 猜最新文件不可靠，文件定位必须以显示名为锚。
- 文件名主干排除用前缀匹配而非精确匹配（`xxx-美化版` ≠ `xxx`）。
- 群聊判定靠标题括号人数（`群名(5)`），不靠名称启发式。

## 测试与打包

**测试**：`cd D:\AI\小漓` 后 `.venv\Scripts\python.exe -m unittest <模块>`。本次核心回归已全绿：`test_visual_backend`（63 用例）、`test_wechat_bot` + `test_task_attachment` + `test_wx_backend`（66 用例）。前端美化改的是 `xiaoli_app\ui`，跑 `test_ui_smoke` + `test_visual_backend`（若误碰后端）。

**打包**：`cd D:\AI\小漓 && .venv\Scripts\python.exe -m PyInstaller --clean --noconfirm 小漓.spec`，产物 `dist\小漓\`（`小漓.exe` + `_internal`）。

**坑**：打包前先确认 `dist\小漓\` 没有被运行中的进程占用（之前 `dist\小漓\wxauto\.rivet\meridian.db` 被某进程锁住导致 COLLECT 清空失败，`rm -rf` 和改名都报 `Device or resource busy`）。若遇到，先退出占用进程再删。

## 下一步：前端美化

### 现状

UI 已有相当基础，不是从零开始：`xiaoli_app\ui\__init__.py` 里有完整主题系统——`THEMES` 定义 5 套配色，`build_qss(theme, wallpaper)` 按主题 + 可选壁纸生成 QSS，毛玻璃用半透明 rgba 卡片实现（Qt QSS 无真高斯模糊）。图标是内嵌的 Lucide SVG（`_icons.py`），导航用 `_load_nav_icon` 运行时着色。

主窗口结构（`main_window.py`）：左侧 200px `QListWidget` 导航（对象名 `navList`）+ 右侧 `QStackedWidget` 六个页面（首页/角色卡/模型/任务/日志/设置）。控件靠 `setObjectName` + `setProperty("tone", ...)` 挂 QSS。

### 美化切入点

- **QSS 主战场**：`xiaoli_app\ui\__init__.py` 的 `_QSS_TEMPLATE` 和 `THEMES`。改配色、间距、圆角、阴影都在这。
- **pages.py 里有 5 处硬编码 `setStyleSheet`**（约 89、237、261、789、1279 行），是历史遗留的散落样式，美化时应统一收进 QSS 模板，别让页面级样式继续漂移。
- **毛玻璃/壁纸**：`build_qss` 已支持壁纸背景 + 半透明卡片。若想强化质感，方向是壁纸模糊（需先高斯模糊壁纸图，再叠半透明卡片）或卡片描边/阴影，而不是改控件布局。

### 注意

- 美化**只动 `xiaoli_app\ui\`**，不要碰 `wx_backend`、`wechat_bot.py`、`xiaoli_bot.py`——微信链路刚跑稳。
- 每改一版跑 `test_ui_smoke`（若在），并用 `browser_debug` 或直接起 GUI 截图看渲染，别只信 QSS 文本没语法错。
- 六个页面里 `pages.py` 最大（63KB），改动前先读对应页的 `__init__` 看控件对象名，QSS 选择器靠这些对象名挂样式。
