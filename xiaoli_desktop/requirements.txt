# 小漓后端依赖（Python 3.12，Windows）
# 安装：python -m venv .venv && .venv\Scripts\pip install -r requirements.txt
# v2.0.0：适配微信 4.1.12+（wxauto4/UIA 通道已弃用，视觉方案唯一通道）

# --- LLM API ---
requests==2.34.2

# --- 图像处理（visual_backend 截图/气泡/头像分析）---
pillow==12.3.0
numpy==2.5.2

# --- OCR（视觉通道文本识别，懒加载）---
rapidocr_onnxruntime==1.4.4

# --- 微信窗口交互（点击/输入/置前）---
PyAutoGUI==0.9.54
pywin32==312            # config_store DPAPI 加密（win32crypt）

# --- 文件内容提取（Office 文档转文字喂 AI）---
comtypes==1.4.16        # Office COM
openpyxl==3.1.5         # Excel
python-docx==1.2.0      # Word
